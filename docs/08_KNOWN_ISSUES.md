# 08_KNOWN_ISSUES.md

**Purpose**: Tracks known issues, technical debt, and unresolved questions across the project. Updated as issues are discovered or resolved.

**Usage**: When an issue is found during implementation or review, add a row. When resolved, move it to the resolved section with the resolution date. This prevents issues from being forgotten across sessions.

---

## Active Issues

| # | Phase | Severity | Description | Discovered | Status |
|---|-------|----------|-------------|------------|--------|
| I-001 | 1 | Medium | **3D hero: geometric placeholder only** - Hero scene uses geometric shapes; real robot GLTF pending AI asset pipeline. | 2026-07-02 | Open |
| I-002 | 1 | Medium | **Real content missing** - Public copy and assets still contain `[BRACKETED]` placeholders; real approved copy, images, video, and 3D models are pending. | Planning | Open |
| I-009 | 1 | **RESOLVED** | **No test coverage — RESOLVED (2026-07-26)** — 117 tests now exist across 7 test files using `node:test`. Coverage: permission evaluator (36), product validation (30), save queue (11), authorization orchestration (12), API route security (13), product editor integration (11), denied-write DB (4). All pass. Critical admin product flow tested end-to-end against disposable PostgreSQL. | Planning | Resolved (2026-07-26 — Step 2.4.3 closure) |
| I-017 | 2 | Medium | **Auth.js User/Session field-name mismatch** — RESOLVED by switching to Better Auth (D-035). Better Auth maps ARIOT's custom fields via `modelName` + `fields` (`image→avatarUrl`, `token→tokenHash`, `ipAddress→ip`, added `emailVerified Boolean`). Auth.js base could not work at runtime without renaming frozen fields; Better Auth was adopted before any real auth data existed. | 2026-07-10 | Resolved (2026-07-10, D-035) |
| I-018 | 2 | High | **Generated-column migration drift blocked the Better Auth migration** - `BlogPost.searchVector`/`Product.searchVector` are generated columns; Prisma's diff emitted invalid `ALTER ... DROP DEFAULT` (Postgres `ERROR 42601`) into the auth migration, failing `migrate deploy`. RESOLVED for Step 2.2.2: the `20260710081202_auth_better_auth_foundation` migration explicitly omits those two spurious statements (documented in the SQL) and now applies cleanly to the local dev DB; runtime DB smoke test PASSED. The root drift (schema declares `searchVector` as plain `tsvector`, init migration created it as `GENERATED ALWAYS AS (...) STORED`) persists and will re-emit the invalid `DROP DEFAULT` on the NEXT `migrate dev`/`db push` for any unrelated schema change — see I-019. | 2026-07-10 | Resolved (2026-07-10, local dev DB; root drift tracked in I-019) |
| I-019 | 2 | **RESOLVED** | **Residual `searchVector` generated-column drift — RESOLVED by corrective C.2 (2026-07-10, final verification 2026-07-25)** — Root cause: init migration created `Product.searchVector` and `BlogPost.searchVector` as `GENERATED ALWAYS AS (...) STORED tsvector` columns. `schema.prisma` declared them as `Unsupported("tsvector")` without the GENERATED expression. Prisma's diff proposed `ALTER COLUMN searchVector DROP DEFAULT` (PostgreSQL ERROR 42601 — invalid on a GENERATED column). Fix: dropped both generated columns and their GIN indexes in corrective migration `corrective_c2_drop_search_vectors`. No user data lost (columns were derivative caches). trgm GIN indexes on name/tagline/description/title/body are preserved and serve the current ILIKE search. Schema.prisma updated to remove fields and `@@index([searchVector])` references. Final verification (2026-07-25): 4-way `prisma migrate diff` all clean (migrations→DB, schema→DB, migrations→schema, fresh replay). Disposable `migrate dev --create-only` probe generated only the test change (no drift). Primary DB verified: row counts unchanged, no searchVector columns, trigram indexes intact. Future `prisma migrate dev` directly proven to work. Stable backup: `C:\Users\princ\backups\ariot\ariot_pre_c2_20260725_201317.dump`. Decision D-062. | 2026-07-10 | Resolved (2026-07-10 — corrective C.2; final verification 2026-07-25) |
| I-025 | 2 | **RESOLVED** | **Permission wildcard mismatch: `products.*` does not authorize `products.read` — RESOLVED by corrective C.1 (2026-07-25)** — All namespace wildcards replaced with explicit permission strings. Central permission catalog at `server/auth/permission-catalog.ts`. Reconciliation CLI applied to local DB. CONTENT_ADMIN now explicitly has `products.read` (10 total explicit permissions). Pure evaluator tests confirm: namespace wildcards do NOT match, explicit strings DO match, global `"*"` allows all. 36 tests pass. Decision D-063. | 2026-07-10 | Resolved (2026-07-25 — corrective C.1) |
| I-026 | 2 | **RESOLVED** | **I-019 was a hard blocker for all planned Phase 2 extended schema additions — RESOLVED by corrective C.2** — I-019 is now resolved (final verification 2026-07-25: `migrate dev --create-only` probe succeeded on disposable DB). Future `prisma migrate dev` runs on a clean or existing local DB will no longer fail. All Phase 2 extended schema additions (HomepageConfig, Promotion, WorkspacePlan, Booking, RdProject, StockMovement, etc.) are now unblocked, subject to C.1 (permission fix) being done first for CONTENT_ADMIN. | 2026-07-10 | Resolved (2026-07-10 — corrective C.2; verified 2026-07-25) |
| I-027 | 2 | Medium | **R2 credentials absent from local `.env` — real dev-bucket smoke test pending (R2-only, no longer blocks Step 2.4.4)** - D-068 (2026-08-18) added the media storage provider abstraction: **Step 2.4.4 is ✅ Done on the `local` provider default** (238/239 node:test pass — 1 opt-in R2 smoke skipped — across 18 test files; `tsc`/`eslint`/`next build` green). The Cloudflare R2 path (`MEDIA_STORAGE_PROVIDER=r2`) is retained, switchable via env, and fail-closed without full `R2_*` + `MEDIA_UPLOAD_TOKEN_SECRET` config, but no R2 credentials exist in the local `.env`, so `tests/media-upload-r2.smoke.test.ts` correctly skips and the R2 end-to-end flow (initiate → presigned PUT → complete → promote → persist) has not run against a real dev bucket. Code paths are exercised by unit/contract + disposable-DB integration tests only. Add credentials per `docs/CLOUDFLARE_R2.md` and run the §5 runbook to verify the R2 provider before any production switch to R2. | 2026-08-17 | Open |
| I-028 | 2 | Low | **`product-details-schema.ts` price transform throws instead of reporting via `ctx.addIssue` — RESOLVED (2026-08-18, Step 2.4.5 final)** — In Zod 4, throwing inside a `.transform` escapes `safeParse` as an unhandled exception instead of returning `success: false`; `product-details-schema.ts` (Step 2.4.3) did `if (num < BigInt(0)) throw new Error('Price cannot be negative.')` and `BigInt(value)` without a digits-only guard, so a malformed price payload (`abc` or `-5`) would bubble out of validation rather than produce a clean 400. Fixed by applying the Step 2.4.5 safe pattern (digits-only regex + `ctx.addIssue` + `return z.NEVER`; empty/null preserved). Regression coverage: 11 new cases in `tests/product-details-validation.test.ts` incl. `assert.doesNotThrow` + `success === false`, plus a 300-digit no-throw parse; full suite 376/376 pass. | 2026-08-18 | Resolved (2026-08-18 — Step 2.4.5 final) |
| I-029 | 2 | Low | **PrismaPg reads `timestamptz` as wall-clock UTC, ignoring the session offset (test-harness / deployment note)** - Observed 2026-08-18 during real-service verification. With a non-UTC Postgres session timezone (e.g. Asia/Dhaka +06), the raw `pg` driver round-trips `timestamptz` correctly while `@prisma/adapter-pg` returns the wall-clock-as-UTC instant, skewing `Product.updatedAt` by the session offset and breaking the variant concurrency token. Test harness works around it by forcing the session to UTC (`?options=-c%20timezone%3DUTC`). Production implication: keep the database session timezone UTC (or document offsets per region) or timestamp comparison tokens become offset-sensitive. Not a code bug in ARIOT services; adapter behavior confirmed empirically. | 2026-08-18 | Open |
| I-011 | 2 | Low | **Auth provider undecided** - Auth.js vs Clerk vs custom; decision deferred to Phase 2 Step 2.2.1. | Planning | Resolved (2026-07-10) |
| I-015 | 1 | Low | **Rate limiter is single-instance only** - `server/rate-limit.ts` is an in-memory fixed-window limiter. It does not share state across serverless instances / multiple nodes; each instance enforces its own 10 req/min/IP window. Adequate for current single-node/preview deploy; needs a shared store (Redis/Upstash) before horizontal scale-out. | 2026-07-08 | Open |
| I-021 | 1 | Low | **Dark SVG placeholders on light cards** - Product/solution/blog/about hero SVGs are still the dark blueprint artwork; on the new light theme they read as dark product photos on white cards (acceptable, but inconsistent with the recolored homepage hero). Recolor to the light palette when the AI asset pipeline (Seedream) lands, or recolor the existing SVGs for full cohesion. | 2026-07-10 | Open |
| I-022 | 1 | Low | **`colorScheme` meta left `'dark'`** - The root-layout viewport `themeColor`/`colorScheme` is intentionally kept `'dark'` so the dark admin console (`app/(admin)`, untouched by the light redesign) keeps correct native control rendering. Public pages are light via the scoped `.theme-light` token override. Revisit if/when the admin console is also lightened. | 2026-07-10 | Open |
| I-023 | 1 | Low | **Remaining brackets in secondary pages** - Some secondary pages (blog/[slug], products/[slug], support articles) still contain bracketed placeholder copy. Low-visibility; brackets will be replaced as real content is approved per CONTENT_STRATEGY §10. | 2026-07-10 | Open |
| I-016 | 1 | Medium | **Phase 1 exit criteria unverified by execution** - Lighthouse >=90, LCP <=2.5s, axe-core zero violations, Google Rich Results validation, and live email delivery were asserted via code analysis, not executed. Build/typecheck/lint pass. | 2026-07-08 | Open |

## Resolved Issues

| # | Phase | Description | Resolution | Resolved |
|---|-------|-------------|------------|----------|
| I-024 | 1 | No browser screenshots captured during visual redesign | Manual browser inspection completed at 390×844 and 360×800. Confirmed: mobile drawer opens from right at correct width (~88vw), no black panel, all nav links and CTAs visible, X/Escape/overlay/nav-link close all work, body scroll locked while open. Homepage, R&D, Workspace, Components, Products, Solutions, About, Contact, Sign-In verified. Admin remains dark and functional. Phase 1 visual redesign marked ✅ complete. | 2026-07-10 |
| I-003 | 1 | Email integration not wired | Resend installed (6.16.0). `server/mail/client.ts` with lazy client + dev stub. Three HTML email templates (contact, quote, newsletter) using ARIOT design tokens. API routes `/api/contact`, `/api/quote`, `/api/newsletter` all send emails. `RESEND_API_KEY` env var optional for dev and required for production delivery. | 2026-07-02 |
| I-004 | 1 | SEO JSON-LD not built | `components/seo/` includes 9 JSON-LD schema components (Organization, WebSite, BreadcrumbList, Product, BlogPosting, TechArticle, FAQPage, Service) wired into marketing pages. | 2026-07-02 |
| I-005 | 1 | Motion library installed but unused | `motion/react` is used by `components/layout/command-palette.tsx` for modal + focus-trap animations. | 2026-07-08 |
| I-010 | 1 | Security headers CSP not hardened | `next.config.ts` now sets CSP plus HSTS, nosniff, frame, referrer, and permissions-policy headers. The current CSP allows `unsafe-inline` and `unsafe-eval` for Next/Tailwind/Turbopack compatibility; stricter nonce-based CSP remains a future hardening target. | 2026-07-02 |
| I-012 | 2 | Database not initialized | Prisma 7.8.0 schema defined, init migration created, and `prisma/seed.ts` seeds roles, one local admin user, categories, products, blog posts, media assets, and seed version setting. | 2026-07-08 |
| I-013 | 1 | Blog/support body content used TypeScript sections instead of MDX | `@next/mdx` installed/configured. MDX content files created for 3 blog posts and 3 support articles. Pages import MDX dynamically. | 2026-07-02 |
| I-014 | 1 | Blog/support article TOC not implemented | `components/marketing/table-of-contents.tsx` built with IntersectionObserver active-section tracking, sticky desktop layout, and collapsible mobile. Wired to blog post and support article pages. | 2026-07-02 |
| I-R01 | - | `PROJECT_BRIEF.md` renamed to `archive/` | Updated cross-references to `00_PROJECT_OVERVIEW.md`. | 2026-07-02 |
| I-R02 | - | Model naming mismatches between master plan and schema | Aligned support/ticket naming in planning docs. | 2026-07-02 |
| I-R03 | - | Phase 5 Category 5.10 steps numbered as 5.9.x | Renumbered to 5.10.1-5.10.6. | 2026-07-02 |
| I-R04 | - | Duplicate Section 10 numbering in `DATABASE_SCHEMA_PLAN.md` | B2B Accounts = Section 10, Conventions checklist = Section 11. | 2026-07-02 |
| I-R05 | - | `TECH_ARCHITECTURE.md` referenced Tailwind config and legacy ESLint files | Corrected to match Tailwind v4 CSS config and `eslint.config.mjs`. | 2026-07-02 |
| I-R06 | - | `AGENTS.md` document index outdated | Updated with current planning and tracking documents. | 2026-07-02 |
| I-R07 | 1 | Products pages listed as incomplete in progress log | Catalog page, detail page, and category page all built; progress log corrected. | 2026-07-02 |
| I-R08 | 1 | Blog page listed as empty shell | Blog index, post page, and seed data all built. | 2026-07-02 |
| I-011 | 2 | Auth provider undecided | Selected **Auth.js (NextAuth v5) + Prisma Adapter** (decision D-034, Step 2.2.1, 2026-07-10). Clerk, custom auth, and Lucia were considered and rejected. No code implemented yet. | 2026-07-10 |
| I-R10 | - | Auth provider undecided (tracking dup of I-011) | Superseded by the D-034 decision in Step 2.2.1. | 2026-07-10 |
| I-R11 | - | Auth.js selected (D-034) | Superseded by Better Auth (D-035, 2026-07-10) before any real auth data existed. | 2026-07-10 |
| I-018 | 2 | Generated-column `searchVector` drift blocked the Better Auth migration (`ERROR 42601` on `ALTER ... DROP DEFAULT`) | The `20260710081202_auth_better_auth_foundation` migration omits the two spurious `DROP DEFAULT` statements (documented inline) and now applies to the local dev DB; runtime DB smoke test PASSED. Root drift persisted as I-019. | 2026-07-10 |

---

**Last Updated**: 2026-08-18
**Open Issues**: 7 | **Resolved**: 21
