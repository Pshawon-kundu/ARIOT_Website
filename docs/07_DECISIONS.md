# 07_DECISIONS.md

**Purpose**: Records every architectural, design, and business decision made during the project. Serves as the single source of truth for *why* things are built the way they are.

**Usage**: When a decision is made (by the user or during planning), add a row to the table below with date, decision, rationale, and alternatives considered. Reference this file when future agents question past choices.

---

## Decisions

| # | Date | Decision | Rationale | Alternatives Considered | Status |
|---|------|----------|-----------|------------------------|--------|
| D-068 | 2026-08-18 | **Media storage provider abstraction (STORAGE-1R evolution): `local` filesystem provider default, `r2` retained** | D-067 implemented a full secure upload pipeline for Cloudflare R2 only, but no R2 credentials exist, so the working system shipped with uploads disabled (D-066). D-068 introduces a `MediaStorageProvider` seam (`name`, `getPublicKey`-based `getPublicUrl`, `checkHealth`) with a production-grade **local provider** (`MEDIA_STORAGE_PROVIDER=local`): persistent absolute root (`MEDIA_LOCAL_ROOT`, default `resolve(cwd,'..','ariot-media-dev')`; production requires it explicitly), provider-neutral storage keys (`tmp/uploads/{yyyy}/{mm}/{id}.{ext}` → promoted `public/products/{images|videos}/{yyyy}/{mm}/{id}.{ext}`), local multipart upload (`POST /api/admin/media/uploads/local`) with the SAME verify/promote/persist pipeline (size caps 10 MB image/200 MB video, real file-signature detection, compensation cleanup), delivery route `app/media/[...segments]` (Node runtime, local-provider-only, immutable cache, Range support, no listing, `tmp/` unreachable). The client picks no provider: `GET /api/admin/media/upload/mode` resolves it server-side. R2 code/route/tests are retained and switchable via env; `r2` remains fail-closed without full R2 config. `persistCompletedAsset` now takes a provider-neutral `PersistPayload` + `cdnUrl` option. No Prisma migration. | (a) Keep R2-only and leave uploads disabled (D-066) — rejected: Step 2.4.4 must ship a working default. (b) Static files in `public/` (not portable, no admin pipeline). (c) Third-party storage (S3/Spaces) — rejected: no creds; provider seam keeps that option open. | Active |
| D-069 | 2026-08-18 | **Product variant rules: free-form option values, uppercase global-unique SKU, `Product.updatedAt` concurrency, soft-delete archive** | Step 2.4.5 (variants tab) freezes the variant data rules without a schema change: (1) **Option groups are free-form `optionValues` key/value pairs** (no `ProductOption` model; no hard-coded option names like Color/Voltage) — normalized server-side (trim; ≤20 keys; key ≤40 chars; value ≤100 chars; `{}` allowed but only once per product), **uniqueness is order-independent** via `optionCombinationKey` (sorted-key JSON fingerprint, active variants only, self-excluded on update). (2) **SKU is normalized to uppercase** (trim + collapse internal whitespace) and **globally unique** — enforced against all `ProductVariant.sku` (including archived) + active `Product.sku`; uniqueness races caught via P2002 → `duplicate/sku`. (3) **Concurrency** reuses the media-tab pattern: `Product.updatedAt` is the aggregate token (`expectedUpdatedAt` in, new token out); stale → 409 conflict; each successful mutation touches `Product.updatedAt` + `updatedBy` and writes an AuditLog (entityType `Product`, entityId productId; `PRODUCT_VARIANT_CREATED`/`_UPDATED`/`_ARCHIVED`); no-op updates succeed with no audit/no token change. (4) **Archive = soft-delete** `deletedAt` (idempotent; archived SKU stays reserved; loader excludes archived). (5) `isDefault=true` clears other active defaults in the same transaction; no auto-forcing on create. (6) `priceMinor` is a non-negative BigInt (BDT/USD; `null` inherits), stock is a plain non-negative Int (inventory movements are 2.4.6). | (a) Hard-coded option names (Color/Voltage...) — rejected: not generic. (b) Case-preserving SKUs — rejected: uppercase avoids case-sensitivity collisions. (c) Hard-delete variants — rejected: breaks audit history + SKU reservation. (d) Dedicated `ProductVariantVersion` table — rejected: over-engineered for v1. | Active |
| D-041 | 2026-07-10 | **Light-first public website, dark admin preserved** | The public marketing site and sign-in page are being corrected to a premium light theme (white surfaces, navy text, refined robotics blue `#2563EB` accent). The admin console (`app/(admin)`) intentionally keeps the original dark tokens. Implementation scopes the light palette behind a `.theme-light` class on the `(marketing)` and `(auth)/sign-in` route groups, re-binding the SAME token CSS variables — so components need no per-file edits and the admin is untouched. | (a) Globally flip tokens to light and force a `theme-dark` wrapper on admin — rejected: requires editing admin files, violating the strict no-admin-change rule. (b) Duplicate a whole parallel light token set — rejected: more drift, more files. (c) Per-component class rewrites — rejected: large, error-prone. | Active |
| D-042 | 2026-07-10 | **Primary nav: Home / R&D / Workspace / Components / Solutions / About; Blog + Support in More menu** | The primary desktop nav must not be overloaded. Blog and Support are important but secondary; a "More" dropdown keeps the bar clean while keeping those routes accessible. Mobile drawer includes all items (scrollable). Contact and Request a Quote are persistent right-side CTAs. | Products as a primary nav item (users find products via Homepage/Solutions); dedicated blog/support primary slots (use More or footer) | Active |
| D-043 | 2026-07-10 | **No invented statistics on the homepage or about page** | ARIOT is an R&D-stage company with no verified deployment counts, customer numbers, or revenue figures. The MetricBand now uses status labels ("R&D", "Planned", "BD", "< 24h") rather than placeholder `[X+]` numbers. Numbers will be added when there is verified evidence to cite. | Keep `[X+]` brackets (visible to public; violates content policy) | Active |
| D-044 | 2026-07-10 | **Mobile drawer uses React portal to escape backdrop-filter containing-block bug** | WebKit/Safari creates a new containing block for `position:fixed` descendants when any ancestor has `backdrop-filter`. The sticky SiteHeader has `backdrop-blur-md` (`backdrop-filter: blur(12px)`), which confined the fixed drawer/backdrop to the header bounds (~60px), producing a narrow 150px strip + large black panel. Fix: `createPortal(overlay, document.body)` renders the drawer at the document root, escaping all parent contexts. Portal root = `fixed inset-0 z-[200]`; panel = `absolute inset-y-0 right-0 w-[88vw] max-w-[360px]`. `theme-light` applied directly to panel. | Leave as fixed in DOM (broken in Safari); use will-change/isolation hacks (fragile) | Active |
| D-045 | 2026-07-10 | **Admin theme boundary via `.theme-admin` class** | Admin shell root div carries `.theme-admin` which explicitly re-declares dark surface tokens. This ensures admin is self-contained and cannot accidentally adopt the public `.theme-light` overrides applied to marketing routes. | Rely on `:root` dark defaults alone (fragile — any `:root` change could leak into admin) | Active |
| D-046 | 2026-07-10 | **Admin density persisted in `localStorage` (key `ariot-admin-density`); default comfortable** | localStorage is safe for a non-sensitive UI preference. Default is 'comfortable' (SSR-safe — avoids hydration mismatch). Value validated on read; invalid/missing values silently default to 'comfortable'. No database model or global state library introduced in this step. | SessionStorage (lost on tab close), server-stored preference (requires DB model + auth linkage — over-engineered for v1), cookie (sends on every request, not needed) | Active |
| D-047 | 2026-07-10 | **Mobile tap-target exception for compact admin density** | Desktop compact mode targets ~32px nav rows. On touch screens (< 1024px), compact mode overrides are suppressed and comfortable (~40px) padding is restored to maintain safe tap targets. This is a CSS-only `@media` override in globals.css — no JS branching. | Reduce mobile rows to 32px (fails WCAG 2.5.8 minimum target size guidance) | Active |
| D-048 | 2026-07-10 | **`AdminStatusChip` distinct from public `Badge` component** | Admin console uses `AdminStatusChip` (dark bg, richer semantic variants: neutral/info/success/warning/danger/pending/inactive). Marketing uses `Badge` (light bg, blue/cyan accent, fewer variants). The two components operate in different visual contexts and must not substitute for each other. A shared primitive would require conditional light/dark logic, adding complexity with no maintenance benefit at this stage. | Extend the public `Badge` with a dark variant (mixes two visual systems in one component) | Active |
| D-049 | 2026-07-10 | **Notifications panel is UI-only; no backend introduced in Step 2.3.4** | The notification model, panel component, and empty state are implemented in client-side TypeScript with no Prisma dependency, no database table, and no API route. `EMPTY_NOTIFICATIONS = []` is a module-level constant. The typed `AdminNotification` interface is the contract for future Phase 3 event-feed integration. Real notifications (new orders, SLA breaches, etc.) require backend work that belongs to the relevant admin module steps. | Add a minimal polling route now (premature; no notifications to poll yet) | Active |
| D-050 | 2026-07-10 | **DataTable is fully controlled; no client-side data fetching** | `DataTable<T>` is a controlled presentation primitive. It never fetches data, runs Prisma queries, manages server state, or assumes an entity type. All rows, sort, filter, and pagination state live in the parent (server component or server action). This enforces a clean separation between data access (server layer) and presentation (client component). | Embed fetch logic in the table (tight coupling, cannot reuse across entity types) | Active |
| D-051 | 2026-07-10 | **No external grid library (Step 2.4.1)** | The admin data-table foundation is implemented with React, native `<table>` semantics, and TypeScript generics. No TanStack Table, AG Grid, or similar library is installed. The requirements for Step 2.4.1 (controlled sort, cursor pagination, loading/empty/error states, density, accessibility) are well within the scope of a hand-built component. Adding a grid library would introduce a significant dependency before requirements have proved it necessary. | Install TanStack Table (adds ~30 kB; over-engineered for current requirements) | Active |
| D-052 | 2026-07-10 | **Sort cycle: none → asc → desc → asc (no clear)** | Clicking a sortable column cycles ascending → descending → ascending. Clicking a different column always starts at ascending on that column. A "clear sort" state is not emitted — the parent always knows which column is sorted. This is predictable and avoids ambiguity in cursor-pagination (an un-sorted result set would need a stable default sort from the server). | Emit null to clear sort (parent must handle undefined default ordering) | Active |
| D-053 | 2026-07-10 | **searchVector (I-019) not used for product search; ILIKE-based fallback** | While I-019 (generated-column drift) is open, the `searchVector Unsupported("tsvector")` column on Product is not queried. Search uses Prisma `contains/mode:'insensitive'` on name/sku/slug (PostgreSQL ILIKE). This is safe, deterministic, and uses indexed trgm columns from the init migration. When I-019 is resolved, upgrading to searchVector is a localized change in `list-products.ts`. | Use searchVector now (would cause Prisma P2001/migration drift errors) | Active |
| D-054 | 2026-07-10 | **Cursor pagination uses product `id` as stable tiebreaker** | Sort order always ends with `{ id: 'asc' }` (forward) or `{ id: 'desc' }` (backward). This ensures deterministic page boundaries even when the primary sort field has duplicate values. The cursor value is the product id — opaque to the UI. Stale/deleted cursors throw Prisma P2001 which is caught and falls back to the first page. | Use composite keyset cursor encoding sort-field value (more efficient but complex) | Active |
| D-055 | 2026-07-10 | **priceMinor (BigInt) serialized to string in DTO** | `Product.priceMinor` is a PostgreSQL BigInt. JSON.stringify does not handle JavaScript BigInt natively. All BigInt values are converted to string in the `toDto` function before being passed through the server→client boundary. The client formats them as `Number(priceMinorStr) / 100` (safe: product prices are well within Number.MAX_SAFE_INTEGER). | Pass as number (risk of precision loss for very large values) | Active |
| D-056 | 2026-07-10 | **Shared product/component catalog — no duplicate model** | Components (dev boards, sensors, motors, etc.) use the same `Product` model as robotics products. Distinction is by category (component-specific categories). No separate `Component` table or duplicated catalog system. This follows the "one catalog model" principle; UX differentiation (components list vs products list) is achieved via category filter presets. | Separate `Component` model (doubles schema complexity, breaks shared editor, inventory, media infrastructure) | Active |
| D-057 | 2026-07-10 | **Structured homepage CMS — no arbitrary HTML injection** | The homepage is editable via structured fields per section (eyebrow, heading, description, image, CTA, featured-entity references, background style choice). No WYSIWYG free-text layout injection, no arbitrary HTML/CSS, no iframe embeds. Design system surfaces are protected. Admin selects from approved `bgStyle` values. Section types are fixed enums. | WYSIWYG free-form editor (arbitrary layout breaks design system, creates security and brand risk) | Active |
| D-058 | 2026-07-10 | **Central promotions engine — one system, placement rules** | All public pages consume promotions through a shared `server/promotions/query.ts` service. Promotions are not hardcoded per page. Placement slots (`HOMEPAGE_HERO`, `WORKSPACE_BOOKING`, etc.) are explicit enums. Priority and time-window rules apply globally. This prevents drift where each page manages its own promotional content independently. | Per-page promotion fields (duplicated data, inconsistent expiry, no cross-page priority) | Active |
| D-059 | 2026-07-10 | **Workspace booking: paymentStatus and bookingStatus are separate enums** | A booking can be CONFIRMED (operationally) with payment PENDING (pay-later plan) or PAID. A booking can be CANCELLED regardless of payment status. Separate enums prevent conflation of the operational lifecycle with the financial lifecycle and simplify state-machine reasoning. | Single combined status (conflates payment and operational state, makes pay-later flows ambiguous) | Active |
| D-060 | 2026-07-10 | **Permission wildcard strategy: adopt explicit permission lists (no glob)** | The current permission helper uses exact string matching. CONTENT_ADMIN has `'products.*'` (namespace wildcard) which does NOT match `requirePermission('products.read')`. Decision: migrate all roles to explicit permission lists (e.g., `['products.read', 'products.write', 'categories.write', ...]`) rather than implementing glob matching. Explicit lists are auditable and prevent unexpected permission escalation. Corrective Step C.1 implements this. | Implement glob matching (less auditable, harder to reason about in security reviews) | Resolved (C.1 complete 2026-07-25) |
| D-063 | 2026-07-25 | **Permission system: global `"*"` only for SUPER_ADMIN; all others use explicit exact-match strings; namespace wildcards prohibited; unknown wildcards fail closed** | Implemented via C.1: central permission catalog (`server/auth/permission-catalog.ts`), pure `hasPermission`/`hasAllPermissions`/`hasAnyPermission` evaluators, reconciliation CLI (`scripts/reconcile-role-permissions.ts`), seed updated, local DB reconciled. 36 focused tests pass. CONTENT_ADMIN now satisfies `products.read`. | Allow namespace wildcards with glob matching (rejected per D-060) | Active |
| D-064 | 2026-07-25 | **Product Details autosave excludes lifecycle fields (status, publishedAt)** | Ordinary Details-tab autosave covers only content fields (name, slug, sku, tagline, description, brand, category, salesType, price, currency). Status transitions (DRAFT→PUBLISHED, PUBLISHED→DRAFT, any→ARCHIVED) are lifecycle operations requiring explicit user confirmation and separate dedicated actions/endpoints. Autosave must never accidentally publish or archive a product. The `status` field is displayed read-only in the Details tab. Future lifecycle actions require a separate mutation with appropriate confirmation UX. | Allow status in autosave with client-side guards (insufficient — client guards are not a security boundary) | Active |
| D-065 | 2026-07-26 | **Shared authorization orchestration factory for production and tests** | Production `updateProductDetails` and authorization contract tests must exercise the SAME code path. A shared `createAuthorizedProductDetailsUpdater({ authorize, executeUpdate })` factory is the single orchestration function. Production injects real `requirePermission` + real executor. Tests inject controlled stubs. No separate test-only authorization implementation exists. Executor failures are sanitized by the factory (never exposes Prisma codes, SQL, stack traces). This eliminates the risk of test coverage diverging from production behavior. | (a) Separate test wrapper using `hasPermission` directly (tests different code path — rejected: proven coverage gap). (b) Mock `requirePermission` via module patching (brittle, doesn't prove factory behavior). (c) Integration-only testing (slow, doesn't isolate orchestration logic). | Active |
| D-066 | 2026-07-26 | **Media tab implements selection/assignment only; upload deferred to storage provider decision** | Step 2.4.4 implements full hero image/video management, gallery management, and existing-media selection using the schema-supported `ProductImage`/`ProductVideo` join models. Signed upload workflow is NOT implemented because no storage provider is configured (D-015 "S3-compatible" frozen). The upload section in the UI is clearly disabled with an explanatory message. No fake upload, no local-filesystem workaround, no client-supplied CDN URL acceptance. Upload implementation will resume when a storage provider decision is made and credentials are available. | (a) Use local filesystem (insecure in production, non-portable). (b) Use placeholder successful upload (deceptive, creates false test coverage). (c) Delay entire step (unnecessary — media selection is fully functional). | Active |
| D-061 | 2026-07-10 | **I-019 is a hard blocker for all new Prisma schema migrations** | The `searchVector Unsupported("tsvector")` generated column on Product and BlogPost causes `ALTER ... DROP DEFAULT (ERROR 42601)` in future `prisma migrate dev` runs. No new Prisma migration may be run until Corrective Step C.2 resolves this. Corrective Step C.2 is the first action required before any Phase 2 extended feature that requires a schema change. | Proceed with new migrations before fixing I-019 (will fail or corrupt migration history) | Resolved (C.2 complete 2026-07-10) |
| D-062 | 2026-07-10 | **Drop GENERATED ALWAYS AS tsvector columns; use trgm GIN indexes for search** | Prisma 7 cannot represent `GENERATED ALWAYS AS (...) STORED` columns without drift. Options: (a) `@default(dbgenerated(...))` — different PG feature (DEFAULT vs GENERATED), still diffs. (b) `@ignore` — can't use with `@@index`. (c) Drop columns — safe: no app reads them, values are derivative caches, trgm GIN indexes on text fields serve ILIKE search. Chosen: smallest change, unblocks all future migrations, preserves all user data. Decision D-062. Future FTS: expression GIN index or search service. | Keep generated columns with @ignore (breaks @@index reference) | Active |
| D-001 | 2026-07-02 | **Framework**: Next.js 16 App Router + RSC | Server Components by default for performance; App Router for nested layouts; React 19 for latest features | Pages Router, Remix, Astro | Frozen |
| D-002 | 2026-07-02 | **Styling**: Tailwind CSS v4 + design tokens | Utility-first for rapid UI; CSS-based config in v4; design tokens for consistency | CSS Modules, Styled Components, vanilla CSS | Frozen |
| D-003 | 2026-07-02 | **Package manager**: pnpm | Fast, disk-efficient, strict dependency resolution | npm, yarn, bun | Frozen |
| D-004 | 2026-07-02 | **Language**: TypeScript strict mode | Type safety prevents runtime errors; required by AGENTS.md | TypeScript loose, JavaScript | Frozen |
| D-005 | 2026-07-02 | **ORM**: Prisma | Type-safe DB access; migration management; good Next.js integration | Drizzle, TypeORM, Kysely | Frozen |
| D-006 | 2026-07-02 | **Database**: PostgreSQL | Relational integrity; JSON support; full-text search; maturity | MySQL, SQLite, MongoDB | Frozen |
| D-007 | 2026-07-02 | **Validation**: Zod at every input boundary | Schema-first validation; TypeScript inference; co-located with handlers | Yup, Joi, Valibot | Frozen |
| D-008 | 2026-07-02 | **Animation**: Motion (Framer Motion successor) | Declarative animations; Spring physics; reduced-motion support | React Spring, CSS animations only, GSAP | Frozen |
| D-009 | 2026-07-02 | **3D**: React Three Fiber + drei + Three.js | React-native 3D; lazy-loadable; community ecosystem | Babylon.js, vanilla Three.js | Frozen |
| D-010 | 2026-07-02 | **UI Components**: shadcn/ui-style, locally-owned | Full control over code; no external dependency; design-system aligned | Radix UI, Headless UI, Chakra UI, Mantine | Frozen |
| D-011 | 2026-07-02 | **Currency**: BDT primary, USD secondary | Primary market is Bangladesh; USD for international visitors | USD primary, single currency | Frozen |
| D-012 | 2026-07-02 | **Payments**: bKash, Nagad, SSLCommerz (Phase 3) | Regional payment ecosystem; Stripe scaffolded for future | Stripe only, PayPal | Frozen |
| D-013 | 2026-07-02 | **Email**: Resend | Simple API; good Next.js integration; free tier | SendGrid, Mailgun, AWS SES | Frozen |
| D-014 | 2026-07-02 | **Auth**: Better Auth (decided Step 2.2.2, see D-035; originally Auth.js per D-034, superseded) | Free/open-source, self-hosted, Prisma-Postgres native, supports ARIOT's custom field names, low lock-in, serves admin + future customer/B2B accounts. | Clerk, custom auth, Lucia, Auth.js | Active |
| D-015 | 2026-07-02 | **Storage**: S3-compatible (Phase 2+) | Local filesystem in dev; DigitalOcean Spaces / Cloudflare R2 in prod | Vercel Blob, Cloudinary | Frozen |
| D-016 | 2026-07-02 | **Analytics**: Vercel Analytics + Plausible | Privacy-friendly; no cookies; lightweight; Vercel-native | Google Analytics, Mixpanel | Frozen |
| D-017 | 2026-07-02 | **AI Assets**: Seedream (images) + Seedance (video) | Premium visual generation; reproducible via prompts + seeds | Midjourney, DALL-E, Runway | Frozen |
| D-018 | 2026-07-02 | **Phase 5 real-time**: SSE over WebSocket | Simpler implementation; sufficient for device telemetry; no WS infrastructure needed | WebSocket, polling | Frozen |
| D-019 | 2026-07-02 | **Admin RBAC**: 4 roles (super_admin, content_admin, support_admin, sales_admin) | Matches team structure; principle of least privilege | Fewer roles, more granular permissions | Frozen |
| D-020 | 2026-07-02 | **Database naming**: PascalCase singular models | Prisma convention; consistent across all docs | snake_case, plural | Frozen |
| D-021 | 2026-07-02 | **Money storage**: priceMinor (BigInt) + currency enum | Never floats for money; smallest unit (paisa/cents); dual currency support | Float, decimal, string | Frozen |
| D-022 | 2026-07-02 | **Soft delete**: deletedAt field on user-facing entities | Reversible; audit trail preserved; FK integrity maintained | Hard delete, archive table | Frozen |
| D-023 | 2026-07-02 | **File naming**: kebab-case for files, PascalCase for exports | Next.js/React convention; clear separation of file vs symbol | camelCase, snake_case | Frozen |
| D-024 | 2026-07-02 | **Blog format**: MDX (Phase 1 static, Phase 2 DB-backed) | Rich content with components; markdown simplicity; code block support | Plain markdown, Sanity, Contentful | Frozen |
| D-025 | 2026-07-02 | **ProductCard**: FeatureCard used instead of dedicated product-card.tsx | FeatureCard already covers all required display properties (icon, eyebrow, title, description, chips, href, cta). Creating a duplicate would violate "no duplicate components" rule. | Separate product-card.tsx component | Active |
| D-026 | 2026-07-02 | **Blog/support body content**: TypeScript sections array instead of MDX | MDX compiler not yet configured; body content is [BRACKETED] placeholders anyway. TypeScript sections render identically. MDX wiring planned when real editorial content is produced. | MDX files in content/, @next/mdx config | Active |
| D-027 | 2026-07-02 | **3D packages installed**: @react-three/fiber, @react-three/drei, three | Step 1.9 explicitly requires these packages. Approved dependency addition per implementation master plan. | Wait for later phase | Active |
| D-028 | 2026-07-02 | **SEO metadata per-page-type variants**: `defineProductMetadata`, `defineBlogMetadata`, `defineSolutionMetadata`, `defineSupportArticleMetadata` | Each page type has distinct OG/Twitter requirements (articles need author/publishedTime/tags; products need category section). Dedicated variants keep callsites clean and enforce correct metadata shape per type. Base `defineMetadata()` unchanged for generic pages. | Single `defineMetadata()` with a `type` discriminator parameter | Active |
| D-029 | 2026-07-08 | **API rate-limiting**: in-memory fixed-window limiter (`server/rate-limit.ts`), 10 req/min/IP, applied to `/api/contact`, `/api/quote`, `/api/newsletter` | Dependency-free, minimal, safe for single-node/preview. No PII/IP logged. Returns HTTP 429 + `Retry-After`. Shared-state scale-out (Redis/Upstash) deferred to Phase 2 (see I-015). | Upstash Ratelimit / Redis token bucket from day one | Active |
| D-030 | 2026-07-08 | **Prisma 7 runtime driver adapter**: `@prisma/adapter-pg` + `pg` | Prisma 7 dropped the built-in Postgres driver; the runtime `PrismaClient` requires a driver adapter (`new PrismaClient({ adapter })`). Chose `@prisma/adapter-pg` (official Pg adapter) over Prisma Accelerate (cloud) or a non-Postgres adapter. Wired in `server/db.ts`. | Prisma Accelerate (managed, adds cloud dependency) | Active |
| D-031 | 2026-07-10 | **Env parsing and DB URL assertion**: `DATABASE_URL` remains optional at base app boot; database-backed code must call the typed `getDatabaseUrl()` helper before creating Prisma. `RESEND_API_KEY` is parsed through `server/env.ts` and production email requires it. | Phase 1/static pages must deploy without DB config, but Phase 2 DB and production email failures must be clear and centralized. | Direct `process.env` reads in `server/db.ts`; making `DATABASE_URL` globally required at boot | Active |
| D-032 | 2026-07-10 | **Server-only convention without installing `server-only` yet**: server modules remain under `server/`; do not import them into client components. `server-only` package may be added later with explicit approval if Next.js bundling risk warrants it. | Avoid adding a package during pre-auth cleanup while documenting the boundary clearly for weaker agents. | Install `server-only` immediately; rely on convention without documenting it | Active |
| D-033 | 2026-07-10 | **Auth block hardening**: Step 2.2.1 is decision-only; provider installation/config, session/UI, RBAC, and admin bootstrap are separate guarded steps. | Auth and RBAC are security-critical and too broad for weaker agents when combined. Splitting the work reduces ambiguity and prevents premature implementation. | Keep the original broad Step 2.2.1-2.2.5 wording | Active |
| D-034 | 2026-07-10 | **Auth Provider: Auth.js (NextAuth v5) + Prisma Adapter** — SUPERSEDED by D-035 | Initially selected as free/open-source + Prisma-friendly. Base integration (2.2.2) exposed a schema incompatibility (custom field names) and upstream guidance now favors Better Auth for new projects; replaced before any real auth users existed. | Auth.js v5 + Prisma Adapter, Custom auth, Clerk | Superseded |
| D-035 | 2026-07-10 | **Auth Provider: Better Auth (replaces Auth.js)** | Free/open-source (MIT), self-hosted, Next.js 16 App Router native, Prisma 7 custom-output compatible, PostgreSQL, supports ARIOT's custom `User`/`Session` field names via `modelName` + `fields` mapping, DB sessions, server-side RBAC preserved. Replaced Auth.js before any real auth data existed. | Auth.js v5, Clerk, custom auth, Lucia | Active |
| D-036 | 2026-07-10 | **RBAC permission wildcard `"*"` for SUPER_ADMIN** | The seed assigns `permissions: ["*"]` to the SUPER_ADMIN role (a single wildcard key rather than enumerated keys). `server/auth/permissions.ts` checks for `"*"` in `ctx.permissions` before per-key matching, granting all permission checks to super admins. This is DB-backed behaviour (not a hardcoded bypass), consistent with the permission matrix in `ADMIN_DASHBOARD_PLAN §3.2`. | Enumerate all permissions for super_admin; reject `"*"` (would break existing seed), use a flag field | Active |
| D-037 | 2026-07-10 | **Bootstrap sets `emailVerified = true` on User creation** | The CLI bootstrap explicitly confirms the admin email via `BOOTSTRAP_ADMIN_CONFIRM=GRANT_SUPER_ADMIN`. This operator-mediated confirmation is treated as equivalent to email verification for the purpose of the Better Auth `emailVerified` flag. Setting it to `true` at creation ensures Better Auth account-linking works on first Google sign-in (some linking paths respect `requireLocalEmailVerified`). No other sign-up flow grants `emailVerified=true` without actual Google verification. | Leave `false` and rely solely on Better Auth setting it during OAuth; risk: some linking paths may require it | Active |
| D-038 | 2026-07-10 | **Admin bootstrap is an offline CLI tool, not a setup endpoint** | No `/setup`, `/admin/init`, or equivalent HTTP route exists. The bootstrap runs once per environment via `pnpm admin:bootstrap --apply`. This eliminates an entire class of web-accessible setup attacks, requires explicit operator action, and keeps secret-handling in the operator's shell (not the web tier). | One-time HTTP setup endpoint (blocked by task requirement); ENV-only root key (no web confirmation step) | Active |
| D-039 | 2026-07-10 | **Account linking: enabled, implicit, same-email-only, no profile overwrite** | `accountLinking.enabled: true` and `disableImplicitLinking: false` allow Better Auth to automatically link a Google account when the verified Google email matches the pre-provisioned User — this is essential for the bootstrap flow. `allowDifferentEmails: false` rejects any different-email Google account from linking. `updateUserInfoOnLink: false` prevents Google from overwriting the admin's name/avatar on link. | Disable implicit linking and force explicit `linkSocial()` call (requires a logged-in session, impossible before first admin login) | Active |
| D-040 | 2026-07-10 | **OAuth tokens encrypted at rest (`encryptOAuthTokens: true`)** | Google OAuth tokens (access, refresh, ID) are encrypted with AES-256-GCM using the existing `BETTER_AUTH_SECRET` before storage in the `Account` table. This protects token material if the database is compromised. No additional key is introduced; no schema migration is required (the encrypted values fit in the existing `TEXT?` columns). Tokens are never logged or exposed to the client. | Store plain-text tokens (existing default, risk: DB breach exposes provider tokens) | Active |
| D-067 | 2026-08-05 | **Cloudflare R2 selected as the S3-compatible storage provider (resolves D-015); STORAGE-1R secure upload architecture** | D-015 froze "S3-compatible" storage with the provider deferred. R2 is S3-API-compatible, has zero egress fees (fits regional budget), supports custom-domain delivery, and keeps the MediaAsset → S3 key model intact. Upload flow: (1) protected initiate (media.write) generates a preallocated MediaAsset ID + canonical keys, returns a 5-minute presigned PUT to the private `tmp/uploads/{yyyy}/{mm}/{id}.{ext}` prefix; (2) browser uploads directly with Content-Type bound; (3) completion (media.write, same user) verifies HEAD (exact size + MIME + ETag), reads ≤64 KiB with IfMatch, detects the actual file signature with file-type (six approved formats only), promotes to immutable `public/products/{images|videos}/{yyyy}/{mm}/{id}.{ext}` with `Cache-Control: public, max-age=31536000, immutable`, reverifies the permanent object, then creates MediaAsset + AuditLog in one transaction. A dedicated `MEDIA_UPLOAD_TOKEN_SECRET` (HMAC-SHA256, 15-minute, jti ≥128 bits) binds mediaAssetId/keys/mime/size/kind/user; MediaAsset.id is the completion idempotency boundary (replay returns the same verified asset). Upload completion is separate from product attachment (attachment stays the existing authorised mutation). Production `r2.dev` disabled; custom-domain WAF default-deny with only `/public/*` anonymously reachable; `/tmp/*` and `/private/*` blocked. Only public product images (jpeg/png/webp/avif) and videos (mp4/webm) are supported; private documents, firmware, and 3D uploads are deferred. Actual file-signature inspection is a mandatory safety layer. No Prisma migration required (MediaAsset already exists; uploads populate it). | Local filesystem (non-portable, insecure in production), DigitalOcean Spaces (same S3 API, but egress fees), Vercel Blob (vendor lock-in, not S3-API compatible), plain presigned upload with no verification (fake success risk) | Active — implemented 2026-08-05; security-correction pass closed 13 implementation defects on 2026-08-17 (`server/storage/`, `tests/upload-complete-integration.test.ts`, `tests/media-upload-r2.smoke.test.ts`, `docs/CLOUDFLARE_R2.md`; real R2 smoke test pending, I-027) |

**D-067 supplementary note (2026-08-17) — security-correction defects closed**: (D1) removed client-controlled `userId`; (D2) removed client-controlled `mediaAssetId`; (D3) initiation schema is strict Zod `{filename, mimeType, sizeBytes, kind}` only; (D4) MediaAsset.id uses `cuid` matching Prisma's `cuid()` default; (D5) presigned PUT (not POST); (D6) all direct package declarations verified in `package.json`; (D7) real `file-type@^21` signature detection in `upload-complete.ts`; (D8) idempotency on `MediaAsset.id` PK (in-DB), not an in-memory JTI Set; (D9) best-effort `DeleteObject` temp cleanup after promote; (D10) ETag-bound copy via `CopySourceIfMatch` (production code matches the precondition); (D11) 12 PostgreSQL integration tests in `tests/upload-complete-integration.test.ts`; (D12) opt-in R2 smoke test in `tests/media-upload-r2.smoke.test.ts`; (D13) test totals corrected from verified runner output: **193 pass (159 unit/contract + 34 PG integration) + 1 opt-in smoke skipped = 194 tests across 14 files** (prior 181 and 34 totals both mis-stated coverage).

> **Status**: Superseded. Auth.js was the initial Step 2.2.1 selection, but its base integration (Step 2.2.2) exposed a Prisma schema incompatibility with ARIOT's custom field names (`emailVerifiedAt`, `avatarUrl`, `tokenHash`, `expiresAt`), and current upstream guidance favors Better Auth for new projects. The provider was changed to **Better Auth (D-035)** on 2026-07-10, before any real authentication users or production auth data existed. The analysis below is retained for audit trail.

- **Decision date**: 2026-07-10
- **Selected provider (original)**: **Auth.js (NextAuth v5) + Prisma Adapter**

### Context

Phase 2 requires admin/CMS access now, and Phase 3 (customer ecommerce) and Phase 5 (B2B/team accounts) require customer and organizational identities later. The project is a small R&D-stage startup (ARIOT Technologies) on a budget. The stack is frozen: Next.js 16 App Router + React 19, Prisma 7 + PostgreSQL 16, TypeScript strict, server-side RBAC planned (`server/auth/permissions.ts`). The schema already mirrors the Auth.js Prisma model shape — `User` (with `passwordHash`, `email`, soft delete), `Session` (`tokenHash`, `expiresAt`), `Role`, and `UserRole` join — and `DATABASE_SCHEMA_PLAN.md` §1 is explicitly designed so each later phase only *adds* identity fields (`Customer`, `Account`, `AccountMember`, `B2B` in Phase 5) without refactoring core types.

### Options considered

1. **Auth.js / NextAuth v5 + Prisma Adapter** — open-source, MIT-licensed, App Router native (`auth()` helper, route handlers), first-class Prisma adapter that maps onto the existing `User`/`Session`/`Role`/`UserRole` models, supports credentials + OAuth (GitHub/Google) + email providers, JWT or DB sessions, no per-seat fee.
2. **Clerk** — hosted auth SaaS, polished prebuilt UI, but per-MAU cost, hosted user store, vendor lock-in, and limited control over the DB-resident RBAC model the project already defines.
3. **Custom authentication** — full in-house passwords/sessions. Maximum control but re-implements battle-tested primitives (argon2id hashing, CSRF, session rotation, OAuth) and raises the security burden unacceptable for a small team.
4. **Lucia (open-source)** — was a strong open-source alternative, but the project is archived/unmaintained as of 2025; adopting an unmaintained auth library now is a long-term maintenance risk, so it was not selected.

### Why Auth.js + Prisma Adapter was chosen

- **Free / open-source first** — MIT licensed, no per-seat or MAU fees; fits a bootstrap budget.
- **Security** — maintained by a large OSS community, supports OAuth (no passwords for admins where possible), integrates with the project's argon2id `passwordHash` field and `Session.tokenHash` strategy, and works with existing HTTP-only/Secure/SameSite cookie + CSRF (Server Action) baseline.
- **Next.js App Router fit** — v5 is built for App Router (`auth()`, middleware optional, route handlers). No CSR-only auth.
- **Prisma/PostgreSQL fit** — the Prisma adapter maps directly onto the already-defined `User`/`Session`/`Role`/`UserRole` models; no schema rework.
- **RBAC suitability** — Auth.js owns *authentication* (who you are); `requireRole`/`requirePermission` helpers in `server/auth/permissions.ts` own *authorization*, layered on the existing `Role.permissions` JSON + `UserRole` join. Clean separation, no client-side-only checks.
- **Admin, customer, and B2B suitability** — same identity core serves `super_admin`/`content_admin`/etc. now, `Customer` in Phase 3, and `Account`/`AccountMember` B2B teams in Phase 5, all without leaving the Prisma data model.
- **Low vendor lock-in / maintainable** — self-hosted on our own Postgres; sessions and users are our data; we can migrate the adapter or swap providers later without losing the user table.
- **Startup practicality / deployment** — runs as ordinary Next.js route handlers on Vercel or any Node host; no external auth service to provision or pay for.

### Why alternatives were rejected

- **Clerk** — rejected: recurring per-MAU cost, hosted user directory (vendor lock-in), and it would duplicate/compete with the already-defined DB RBAC model. Only justifiable if ARIOT later wants to offload all auth engineering; not warranted at this stage.
- **Custom auth** — rejected: unacceptable security burden for a small team (must hand-roll hashing, session storage, CSRF, OAuth, brute-force protection) and slower to ship. Approved only as a fallback if a future requirement cannot be met by Auth.js.
- **Lucia** — rejected: archived/unmaintained upstream; adopting it now creates a future maintenance/security liability.

### Security implications

- Admin sign-in should prefer **OAuth (GitHub/Google)** where possible; credentials (email + argon2id `passwordHash`) only where required.
- Passwords never stored in plaintext; `passwordHash` already nullable for OAuth-only users.
- Sessions: DB-backed (`Session` table with `tokenHash`, `expiresAt`) or JWT — decision deferred to Step 2.2.2, but tokens stay HTTP-only, Secure, SameSite=Lax, with quarterly secret rotation (per `TECH_ARCHITECTURE.md` §11).
- RBAC enforced **server-side only** (`(admin)` layout + every server action) via `requireRole`/`requirePermission`; no admin data behind client-only checks.
- Public auth mutation endpoints (`/api/auth/*`, password-reset) must be rate-limited (extends existing `server/rate-limit.ts`).
- No real credentials, hashes, session tokens, OAuth secrets, or admin emails committed to repo, `.env.example`, seeds, or docs.

### Cost implications

- **$0** for the auth library itself. Only infra we already pay for (Postgres, Next.js host). No Clerk MAU fees. Optional paid OAuth providers are not required.

### Vendor lock-in implications

- Minimal. User/session/role data lives in our Postgres via the Prisma adapter. If Auth.js is ever replaced (e.g., by a custom layer or different provider), the `User`/`Session`/`Role`/`UserRole` tables remain the source of truth and migrate cleanly.

### Future migration considerations

- The adapter boundary is the only Auth.js-specific coupling. Swapping providers means re-implementing the `auth()`/`getSession()` surface and the `server/auth/permissions.ts` session read — the DB models stay.
- Phase 3 (`Customer`) and Phase 5 (`Account`, `AccountMember`, B2B seats) extend the **same** `User` identity; no new auth provider needed.

### Implementation notes for later steps

- **Step 2.2.2**: install `next-auth@beta` (v5) + `@auth/prisma-adapter`; add env names (`AUTH_SECRET`, OAuth client IDs/secrets) to `server/env.ts` + `.env.example`; create the minimal `auth.ts` config + `/api/auth/[...nextauth]/route.ts`. Do **not** build UI/RBAC yet.
- The existing `User`/`Session`/`Role`/`UserRole` models already match the adapter shape; verify field names during 2.2.2 and adjust the adapter mapping (not the frozen core types) if needed.
- **Step 2.2.3**: smallest sign-in flow (OAuth-first, optional credentials), callback/error handling, session verification.
- **Step 2.2.4**: `server/auth/permissions.ts` from `ADMIN_DASHBOARD_PLAN.md` §3 + `requireRole`/`requirePermission` server-only guards; guard `(admin)` routes only after session reads work.
- **Step 2.2.5**: bootstrap the first `super_admin` via OAuth allowlist or an env-initialized pending `super_admin` activated through a one-time setup flow — no real credentials in repo.
- Prefer middleware only if the chosen session strategy requires it; otherwise the smallest server-side guard that proves the boundary.
- Record the final bootstrap method back in this decision log (D-034) during 2.2.5.

---

## D-035 — Auth Provider Selection: Better Auth (replaces Auth.js, Step 2.2.2)

- **Decision date**: 2026-07-10
- **Selected provider**: **Better Auth** (MIT, self-hosted) + official Prisma adapter (`better-auth/adapters/prisma`)
- **Supersedes**: D-034 (Auth.js / NextAuth v5)

### Context

Step 2.2.2 installed Auth.js v5 beta + `@auth/prisma-adapter`. The base integration compiled but could **not** work at runtime: the Auth.js adapter expects `User.emailVerified`/`image` and `Session.sessionToken`/`expires`, while ARIOT's schema uses `emailVerifiedAt`/`avatarUrl`/`tokenHash`/`expiresAt`. Renaming the frozen, already-migrated-and-seeded fields was a risky change. At this point ARIOT has **no live authentication users and no production authentication data**, so switching providers carries zero migration cost.

### Why Better Auth was selected

- **Free / open-source + self-hosted** — MIT licensed, no per-seat/MAU fees, runs on our own Postgres.
- **Next.js 16 App Router native** — official `toNextJsHandler` for `app/api/auth/[...all]/route.ts`; docs explicitly cover Next.js 16 (proxy replaces middleware).
- **Prisma 7 compatible** — works with the custom Prisma client output directory (`@/lib/generated/prisma/client`); adapter reads the client from that path.
- **PostgreSQL compatible** — `prismaAdapter(prisma, { provider: 'postgresql' })`.
- **Custom model + field names supported** — via `modelName` (e.g. `"User"`, `"Session"`) and `fields` mapping (e.g. `image → avatarUrl`, `token → tokenHash`, `ipAddress → ip`). Inspected the adapter source: it queries `prisma[modelName]` and resolves columns via `getFieldName`, so ARIOT's PascalCase models and business field names are preserved.
- **Database sessions** — default strategy; fits the existing `Session` model.
- **Preserves ARIOT RBAC** — Better Auth owns *authentication*; ARIOT's `Role`/`UserRole`/`permissions` JSON model remains the authorization source (`server/auth/permissions.ts`, later step). Not replaced by a comma-separated role field.
- **Future customer + B2B accounts** — same `User` identity core; Phase 3 `Customer` and Phase 5 `Account`/`AccountMember` extend the model without a new provider.
- **Low vendor lock-in** — self-hosted; user/session/role data stays in our Postgres.

### Why Auth.js was superseded (not retained merely because installed)

- The installed base could not function at runtime without renaming frozen fields or carrying a permanent adapter field mismatch (I-017).
- Current upstream guidance recommends Better Auth for new projects; Better Auth's Prisma adapter has first-class custom table/field mapping that fits ARIOT's conventions directly.
- Switching now (before any real auth data) avoids a costly later migration.

### Comparison vs installed Auth.js v5 (for this repo)

| Criterion | Auth.js v5 (installed) | Better Auth (selected) |
|---|---|---|
| Free / OSS | Yes (MIT) | Yes (MIT) |
| Self-hosted | Yes | Yes |
| Next.js 16 App Router | Yes | Yes (explicit docs) |
| Prisma 7 custom output | Yes | Yes |
| PostgreSQL | Yes | Yes |
| Custom field/table names | Rigid (expects exact names) | First-class (`modelName` + `fields`) |
| Compat with ARIOT schema | **No** (field mismatch) | **Yes** (mapped) |
| DB sessions | Yes | Yes (default) |

### Security implications

- Session token + `BETTER_AUTH_SECRET` (HMAC). `secret` read through typed `server/env.ts`; HTTP-only, Secure, SameSite cookies per `TECH_ARCHITECTURE.md` §11.
- OAuth-first admin sign-in preferred; credentials only where required (later step).
- RBAC enforced server-side only via `requireRole`/`requirePermission`; no client-only admin protection.
- Auth mutation endpoints must be rate-limited (extends `server/rate-limit.ts`).
- No real secrets/credentials in repo, `.env.example`, seeds, or docs.

### Cost / lock-in / migration

- **$0** library cost; runs on existing Postgres + Next.js host.
- Minimal lock-in: data lives in our Postgres. The adapter boundary is the only Better Auth-specific coupling; DB models stay if ever swapped.

### Schema changes made (Step 2.2.2)

- Added `emailVerified Boolean @default(false)` to `User` (Better Auth flag; `emailVerifiedAt` retained as ARIOT business field).
- Added `updatedAt` to `Session`.
- Redesigned `Account` to Better Auth's shape (`accountId`, `providerId`, `accessToken*`, `refreshToken*`, `idToken`, `*ExpiresAt`, `scope`, `password`).
- Replaced `VerificationToken` with `Verification` (`identifier`, `value`, `expiresAt`).
- Mapped fields: `user.image → avatarUrl`, `session.token → tokenHash`, `session.ipAddress → ip`.
- Kept PascalCase model names (`User`, `Session`, `Account`, `Verification`) and set `modelName` so the adapter resolves them.

### Implementation notes for later steps

- **Step 2.2.3** (done 2026-07-10): Google is the only configured OAuth provider. `socialProviders.google` is enabled only when both `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` are set, with `disableSignUp: true` (no public account creation) and identity-only scopes. `trustHost` removed in favor of explicit `baseURL` + `trustedOrigins`. Shared React client at `lib/auth-client.ts`; sign-in route `app/(auth)/sign-in/`; minimal `getSession()` helper (authn only). LIVE Google OAuth not tested (no creds in env).
- **Step 2.2.4**: `server/auth/permissions.ts` + `requireRole`/`requirePermission` server-only guards.
- **Step 2.2.5**: bootstrap first `super_admin` via OAuth allowlist or env-initialized pending record — no real credentials in repo.
- Apply the additive auth schema to the database via a migration; the Better Auth migration `20260710081202_auth_better_auth_foundation` is GENERATED and APPLIED to the LOCAL dev DB, with a runtime DB smoke test PASSED. I-018 is resolved for this step; the residual `searchVector` generated-column drift (Prisma emits an invalid `ALTER ... DROP DEFAULT` on generated columns) is tracked as I-019 and must be fixed before any OTHER `prisma db push`/`migrate dev` schema change. (Also fixed `server/env.ts` so `BETTER_AUTH_SECRET`/`BETTER_AUTH_URL` are actually parsed.)

---

**Last Updated**: 2026-08-18
**Total Decisions**: 64
