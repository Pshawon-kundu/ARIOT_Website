# 06_PROGRESS_LOG.md

**Purpose**: Tracks implementation progress across all 5 phases. Updated after every significant implementation step.

**Usage**: At the end of every implementation turn, update the relevant phase table with the step completed, date, and any notes. This is the authoritative record of what has been built.

---

## Phase 1 — Premium Public Website

| Step | Category | Status | Completed | Notes |
|------|----------|--------|-----------|-------|
| 1.1.1 | Project Scaffold | ✅ Done | 2026-07-02 | Verified complete — Next.js 16.2.4, TypeScript strict, Tailwind v4, ESLint 9+ flat config. Build passes (8.0s, 20 routes). All config files present.
| 1.1.2 | Configure Tooling | ✅ Done | 2026-07-02 | Husky 9.1.7 + lint-staged 17.0.8 pre-commit hook, @next/bundle-analyzer 16.2.10, Prettier configured. `pnpm lint` zero warnings. |
| 1.1.3 | Path Aliases & Env | ✅ Done | 2026-07-02 | @/ alias in tsconfig.json, .env.example, server/env.ts with Zod validation. |
| 1.1.4 | Folder Structure | ✅ Done | 2026-07-02 | All dirs per TECH_ARCHITECTURE.md: app/, components/, lib/, server/, features/, docs/, public/, content/, tests/, scripts/. .gitkeep added to empty dirs. |
| 1.2.1 | Design Tokens | ✅ Done | — | lib/design-tokens.ts |
| 1.2.2 | Typography System | ✅ Done | — | next/font configured |
| 1.2.3 | Button | ✅ Done | — | components/ui/button.tsx |
| 1.2.4 | Input/Textarea/Select/Label | ✅ Done | — | components/ui/ |
| 1.2.5 | Card/Badge/Separator/Container/Section | ✅ Done | — | components/ui/ |
| 1.2.6 | SkipLink | ✅ Done | — | components/ui/skip-link.tsx |
| 1.2.7 | Motion Tokens | ✅ Done | — | lib/motion/tokens.ts |
| 1.2.8 | cn() Utility | ✅ Done | — | lib/utils/cn.ts |
| 1.3.1 | Root Layout | ✅ Done | — | app/layout.tsx |
| 1.3.2 | Global Header | ✅ Done | — | components/layout/site-header.tsx |
| 1.3.3 | Global Footer | ✅ Done | — | components/layout/site-footer.tsx |
| 1.3.4 | Marketing Layout | ✅ Done | — | app/(marketing)/layout.tsx |
| 1.3.5 | NavLink | ✅ Done | — | components/layout/nav-link.tsx |
| 1.4.1 | HeroShell | ✅ Done | — | components/marketing/hero-shell.tsx |
| 1.4.2 | Homepage Hero | ✅ Done | — | app/(marketing)/page.tsx |
| 1.4.3 | Trust Strip | ✅ Done | — | components/marketing/logo-strip.tsx |
| 1.4.4 | Feature Stack | ✅ Done | — | components/marketing/feature-stack.tsx |
| 1.4.5 | Feature Grid | ✅ Done | — | components/marketing/feature-grid.tsx |
| 1.4.6 | Solutions Grid | ✅ Done | — | component variant |
| 1.4.7 | Metric Band | ✅ Done | — | components/marketing/metric-band.tsx |
| 1.4.8 | Blog Teaser | ✅ Done | — | components/marketing/media-showcase.tsx |
| 1.4.9 | Testimonial | ✅ Done | — | components/marketing/testimonial.tsx |
| 1.4.10 | CTA Band | ✅ Done | — | components/marketing/cta-band.tsx |
| 1.4.11 | Compose Homepage | ✅ Done | — | All sections assembled |
| 1.5.1 | Product Seed Data | ✅ Done | — | app/(marketing)/products/_data.ts |
| 1.5.2 | Product Card | ✅ Done | 2026-07-02 | FeatureCard (components/marketing/feature-card.tsx) used in place of a dedicated product-card.tsx — equivalent functionality, no duplication. |
| 1.5.3 | Catalog Page | ✅ Done | 2026-07-02 | app/(marketing)/products/page.tsx — full catalog with filter sidebar, category chips, FeatureGrid. |
| 1.5.4 | Product Detail | ✅ Done | 2026-07-02 | app/(marketing)/products/[slug]/page.tsx — hero gallery, buy box, specs table, downloads, related products. |
| 1.5.5 | Category Page | ✅ Done | 2026-07-02 | app/(marketing)/products/category/[slug]/page.tsx — filtered product grid, hero with category name + tagline, CTA band. generateStaticParams for all 6 slugs. |
| 1.5.6 | Category Seed Data | ✅ Done | 2026-07-02 | CATEGORIES array added to app/(marketing)/products/_data.ts — 6 entries with slug, name, tagline, description. getCategoryBySlug + getProductsByCategory helpers. |
| 1.6.1 | Solution Seed Data | ✅ Done | 2026-07-02 | features/solutions/_data.ts — 6 solutions (smart-factory, smart-agriculture, smart-city, energy-utilities, education, custom). Each with stats, approach, techStack, relatedProductSlugs. |
| 1.6.2 | Solutions Index | ✅ Done | 2026-07-02 | app/(marketing)/solutions/page.tsx — hero, industry grid, approach stack, timeline, case study placeholder, CTA. |
| 1.6.3 | Solution Detail | ✅ Done | 2026-07-02 | app/(marketing)/solutions/[slug]/page.tsx — hero with icon/industry, metrics, approach feature stack, tech stack chips, case study placeholder, related products, CTA. generateStaticParams. |
| 1.6.4 | Timeline Component | ✅ Done | 2026-07-02 | components/marketing/timeline.tsx — vertical timeline with cyan markers, numbered mode. |
| 1.7.1 | About Page | ✅ Done | 2026-07-02 | app/(marketing)/about/page.tsx — hero, manifesto, metrics, story timeline, team grid, certifications, press, CTA. |
| 1.7.2 | Blog Index | ✅ Done | 2026-07-02 | app/(marketing)/blog/page.tsx — featured hero, categories, grid, innovation lab, newsletter CTA. |
| 1.7.3 | Blog Post | ✅ Done | 2026-07-02 | app/(marketing)/blog/[slug]/page.tsx — hero with cover placeholder, article body, author bio, related posts, newsletter CTA. generateStaticParams. |
| 1.7.4 | Blog Seed Data | ✅ Done | 2026-07-02 | features/blog/_data.ts — 3 seed posts (autonomous robots, IoT agriculture, regional robotics) with sections as [BRACKETED] content. |
| 1.7.5 | Support Hub | ✅ Done | 2026-07-02 | app/(marketing)/support/page.tsx — search, category grid, manuals/FAQ, CTA band. |
| 1.7.6 | Support Article | ✅ Done | 2026-07-02 | app/(marketing)/support/article/[slug]/page.tsx — article body, sidebar, helpful feedback, related articles, CTA. 3 seed articles in features/support/_data.ts. |
| 1.7.7 | Support Static Pages | ✅ Done | 2026-07-02 | app/(marketing)/support/manuals/page.tsx and firmware/page.tsx — manuals list and firmware release list with placeholder links. |
| 1.7.8 | Contact Page | ✅ Done | — | app/(marketing)/contact/page.tsx |
| 1.7.9 | Quote Page | ✅ Done | 2026-07-02 | app/(marketing)/quote/page.tsx — multi-step form display, "What happens next" timeline, side panel. |
| 1.8.1 | Legal Layout & Routing | ✅ Done | 2026-07-02 | app/(marketing)/legal/layout.tsx — breadcrumb header, legal nav footer. app/(marketing)/legal/page.tsx — index linking all 5 legal pages. |
| 1.8.2 | Legal Page Content | ✅ Done | 2026-07-02 | 5 legal pages (privacy, terms, cookies, warranty, shipping) — all with [BRACKETED] placeholder content. components/marketing/legal-page.tsx shared prose component. |
| 1.8.3 | Footer Legal Links | ✅ Done | 2026-07-02 | LEGAL_LINKS already in site-footer.tsx prior to this session. Legal pages now created so links resolve. |
| 1.9.1 | R3F Setup & Lazy-Load | ✅ Done | 2026-07-02 | @react-three/fiber, @react-three/drei, three installed. components/three/r3f-wrapper.tsx — lazy IntersectionObserver-gated wrapper with Suspense skeleton fallback. |
| 1.9.2 | Hero 3D Scene | ✅ Done | 2026-07-02 | components/three/hero-scene.tsx — geometric robot placeholder (disc chassis, dome, cyan sensor ring, LiDAR turret). Scroll dolly (first 20%), DPR [1,1.75], slow rotation useFrame. |
| 1.9.3 | Mobile Fallback | ✅ Done | 2026-07-02 | components/three/hero-3d-client.tsx — useReducer-based viewport detection. < 768px shows styled placeholder (Seedance pending). Desktop loads R3FWrapper. |
| 1.10.1 | Metadata Helpers | ✅ Done | 2026-07-02 | lib/seo/metadata.ts — added defineProductMetadata, defineBlogMetadata, defineSolutionMetadata, defineSupportArticleMetadata variants. Updated products/[slug], blog/[slug], solutions/[slug], support/article/[slug] to use page-type-specific metadata (OG type, authors, publishedTime, tags). |
| 1.10.2 | JSON-LD Components | ✅ Done | 2026-07-02 | components/seo/ — 9 JSON-LD components (JsonLd, Organization, WebSite, BreadcrumbList, Product, BlogPosting, TechArticle, FAQPage, Service) + barrel index. Wired into 13 pages: homepage (Org+WebSite), about (Org+BreadcrumbList), products catalog (BreadcrumbList), product detail (Product+BreadcrumbList), product category (BreadcrumbList), blog index (BreadcrumbList), blog post (BlogPosting+BreadcrumbList), solutions index (BreadcrumbList), solution detail (Service+BreadcrumbList), support hub (FaqPage+BreadcrumbList), support article (TechArticle+BreadcrumbList). |
| 1.10.3 | Sitemap & Robots | ✅ Done | 2026-07-02 | app/sitemap.ts — enumerates 15 static routes + 6 products + 6 categories + 6 solutions + 3 blog posts + 3 support articles (39 total URLs). app/robots.ts — allows all public marketing routes, disallows /admin, /account, /auth, /api, /checkout, /cart. |
| 1.10.4 | OG Image Generation | ✅ Done | 2026-07-02 | app/api/og/route.tsx — dynamic OG image generation via Edge runtime + next/og. Accepts `title` and `type` (product/blog/solution/support) search params. Renders ARIOT branding with Space Grotesk display font, Inter body font, design system colors (#08090B bg, #E4E8EE text, #3DD8F7 cyan accent), page-type-specific badge, gradient accent line, and footer. lib/seo/metadata.ts updated — all 5 metadata variants (base, product, blog, solution, support) now generate dynamic OG URLs via /api/og with appropriate type badges. Root layout OG/Twitter images also updated. |
| 1.11.1 | Form Submission Hook | ✅ Done | — | features/forms/use-form-submit.ts |
| 1.11.2 | Contact Form | ✅ Done | — | features/forms/contact-form.tsx |
| 1.11.3 | Quote Form | ✅ Done | — | features/forms/quote-form.tsx |
| 1.11.4 | Newsletter Form | ✅ Done | — | features/forms/newsletter-form.tsx |
| 1.11.5 | API: Contact | ✅ Done | — | app/api/contact/route.ts |
| 1.11.6 | API: Quote | ✅ Done | — | app/api/quote/route.ts |
| 1.11.7 | API: Newsletter | ✅ Done | — | app/api/newsletter/route.ts |
| 1.11.8 | Email Service (Resend) | ✅ Done | 2026-07-02 | Installed resend 6.16.0. Created server/mail/client.ts — Resend client singleton with lazy init, dev stub fallback (console.log when API key missing), sendEmail helper. Created server/mail/templates/ — contact-notification.ts (team inbox: name, company, email, phone, topic, message), quote-notification.ts (sales inbox: industry, timeline, product category, use case, contact details), newsletter-welcome.ts (subscriber: welcome copy, expectations, CTA). Updated API routes: app/api/contact/route.ts, quote/route.ts, newsletter/route.ts — all now send emails via Resend. Added RESEND_API_KEY to server/env.ts (optional) and .env.example. |
| 1.12.1 | Security Headers | ✅ Done | 2026-07-02 | next.config.ts — added hardened Content-Security-Policy header (default-src 'self', script-src 'self' 'unsafe-eval' 'unsafe-inline' + Vercel Analytics, style-src 'self' 'unsafe-inline' + Google Fonts, font-src 'self' + Google Fonts + data:, img-src 'self' data: blob: https:, media-src 'self' blob:, connect-src 'self' + Vercel Analytics endpoints, frame-ancestors 'none', base-uri 'self', form-action 'self'). Existing headers retained: HSTS (63072000s + includeSubDomains + preload), X-Content-Type-Options nosniff, X-Frame-Options DENY, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy (camera/mic/geo/payment/usb/mag/gyro all denied). |
| 1.12.2 | Analytics Setup | ✅ Done | 2026-07-03 | Installed `@vercel/analytics` 2.0.1 (already present). Added Plausible Analytics — deferred script tag in `app/layout.tsx` via `next/script`, gated on `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` env var. Added `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` to `server/env.ts` (optional) and `.env.example`. Updated CSP in `next.config.ts` — added `https://plausible.io` to `script-src` and `connect-src`. Created `lib/analytics.ts` — typed `trackEvent()` wrapper around `@vercel/analytics/react` track with typed event registry (6 events: Contact Form Submitted, Quote Form Submitted, Newsletter Subscribed, CTA Clicked, Product Viewed, Product Card Clicked). Wired tracking into all 3 forms (`contact-form.tsx`, `quote-form.tsx`, `newsletter-form.tsx`) — fire on successful submit. Created `features/analytics/product-grid-tracker.tsx` — client event-delegation wrapper for product catalog (tracks Product Card Clicked + CTA Clicked). Created `features/analytics/product-view-tracker.tsx` — client on-mount tracker for product detail pages (Product Viewed). Wired into products catalog page and product detail page. |
| 1.12.3 | Error Boundary & 404 | ✅ Done | — | app/error.tsx, app/not-found.tsx |
| 1.12.4 | Performance Audit | ✅ Done | 2026-07-03 | Proactive performance audit via code analysis (Lighthouse CLI not available in environment). Findings: (1) Font loading optimal — `next/font/google` with `display: 'swap'`, minimal subsets, minimal weights. (2) No raw `<img>` tags — all image-ready via Next.js Image when assets arrive. (3) Script loading optimal — Plausible deferred via `next/script`, Vercel Analytics managed. (4) Three.js barrel import fixed — `import * as THREE from 'three'` replaced with `import type { Group } from 'three'` in `components/three/hero-scene.tsx` (tree-shaking improvement for lazy chunk). (5) CSS clean — Tailwind v4 build-time import, 224 lines, no render-blocking `@import`. (6) Metadata lean — no duplicates, proper `metadataBase`. (7) 3D lazy-loading excellent — `dynamic()` ssr:false + IntersectionObserver gate + DPR clamped. (8) `prefers-reduced-motion` supported. Build verified: 48 routes, typecheck pass, lint pass. |
| 1.12.5 | Accessibility Audit | ✅ Done | 2026-07-03 | Proactive WCAG 2.2 AA audit via code analysis (axe-core CLI not available). Found and fixed 4 issues: (1) **Newsletter form inline label** (WCAG 1.3.1, 3.3.2, 4.1.2) — `newsletter-form.tsx` inline variant had no accessible name on email input. Added `label="Email"` with `labelClassName="sr-only"`. Added `labelClassName` prop to `FormField` component. (2) **Products page heading hierarchy** (WCAG 1.3.1) — h1→h3 skip (Catalog filters CardTitle). Added `as` prop to `CardTitle` component; products page now uses `as="h2"`. (3) **Mobile drawer focus trap + tab order** (WCAG 2.4.3) — Drawer had no focus trap when open and leaked into tab order when closed. Added keyboard focus trap (Tab/Shift+Tab cycling), `aria-hidden`+`inert` when closed, focus return to trigger button on close. (4) **Placeholder divs without role=img** — Contact page map placeholder and solutions page media placeholder missing `role="img"` + `aria-label`. Added consistent with rest of codebase. All color contrast ratios verified ≥4.5:1 (steel-400 on bg-base: 5.34:1, cyan-400 on bg-base: 11.73:1). Focus indicators, skip link, reduced motion, tab order all verified clean. |
| 1.12.6 | Command Palette | ✅ Done | 2026-07-03 | components/layout/command-palette.tsx — Ctrl/Cmd+K modal shell with search input, navigation suggestions, Motion animation, focus trap, body scroll lock. Wired into marketing layout. |
| 1.13.1 | Generate Hero Assets | ✅ Done | 2026-07-03 | Created prompt JSON (Seedream still + Seedance video specs), SVG placeholder hero assets (21:9 desktop, 9:16 mobile, poster), wired hero Image layer into HeroShell (priority LCP), updated mobile fallback in hero-3d-client to use poster asset. SVGs follow ARIOT design language — will be replaced when real Seedream/Seedance assets are generated. |
| 1.13.2 | Generate Product/Solution Images | ✅ Done | 2026-07-03 | Full Phase 1 asset generation: 6 product hero SVGs + prompt JSONs (all SKUs); 6 solutions scene SVGs + prompt JSONs; solutions-index-hero SVG + prompt JSON; 3 blog cover SVGs + prompt JSONs; blog build-log feature SVG + prompt JSON; about-hero-lab SVG + prompt JSON; 3 engineering pillar SVGs + 2 prompt JSONs; 4 home product prompt JSONs; home-engineering-navigation prompt JSON. Wired into 6 pages: product detail MediaGallery, about hero card, FeatureStack FeatureMedia, blog build-log, solutions case study, solutions detail case study. All SVGs use ARIOT design language (graphite + cyan). Typecheck + lint pass. Remaining: home page tile SVGs (FeatureCard uses Lucide icons by design, D-025). |
| 1.13.3 | Optimize All Images | ✅ Done | 2026-07-03 | Audited all 11 Image components across 8 files. All sizes correct per layout. Added priority to product detail hero Image (LCP improvement). SVGs are vector-native — AVIF/WebP conversion deferred until real Seedream raster assets generated (step 1.13.1/1.13.2 note). typecheck + lint pass. Phase 1 complete (74/74 steps). |

---

## Phase 2 — Product CMS / Admin

| Step | Category | Status | Completed | Notes |
|------|----------|--------|-----------|-------|
| 2.1.1 | Database Setup | ✅ Done | 2026-07-03 | prisma@7.8.0 + @prisma/client@7.8.0 installed. PostgreSQL 16 installed via winget, `ariot` database created. `.env` configured with DATABASE_URL. `server/env.ts` updated to validate DATABASE_URL. `npx prisma db push` succeeds — connection verified. |
| 2.1.2 | Database Setup | ✅ Done | 2026-07-03 | Core schema models defined in prisma/schema.prisma: User, Session, Role, UserRole (Identity §1); Category, Product, ProductVariant, ProductImage, ProductVideo, ProductDownload, ProductRelation (Catalog §2). 8 enums, all FK relations with cascade policies, composite PK on UserRole + ProductRelation, indices on every FK and hot-where columns, cuid() IDs, createdAt/updatedAt/deletedAt on user-facing entities, BigInt money + Currency enum. `npx prisma validate`, `prisma generate`, `prisma db push` all pass. `pnpm typecheck` zero errors. |

---

## Phase 3 — Ecommerce

*Not started. All steps ⬜.*

---

## Phase 4 — Support / Ticket System

*Not started. All steps ⬜.*

---

## Phase 5 — Customer Dashboard + IoT

*Not started. All steps ⬜.*

---

**Last Updated**: 2026-07-10
**Phase 1 Progress**: 74 of 74 steps complete (100%) + I-003 + I-004 + I-010 + I-005 + I-012 resolved
**Phase 2 Progress**: 8 of 43 steps complete (19%)
**Phase 1 Closeout**: 2026-07-08 — security (rate limiting) + env (DATABASE_URL optional) + stale-doc fixes applied; verification green.

## 2026-07-07 Progress Addendum

| Step | Category | Status | Completed | Notes |
|------|----------|--------|-----------|-------|
| 2.1.5 | Database Setup | ✅ Done | 2026-07-07 | Created initial Prisma migration `prisma/migrations/20260707160338_init/migration.sql` from the frozen schema. The existing local database was unmanaged from earlier `db push` work and all existing tables had zero rows, so it was reset before applying the init migration. Patched the migration to enable `citext` and `pg_trgm`, use generated `tsvector` columns for product/blog search, and revoke public UPDATE/DELETE grants on `AuditLog`. Added `prisma/seed.ts` and configured `prisma.config.ts` seed command. Seed inserts 4 roles, 1 local admin user, 6 categories, 6 products migrated from Phase 1 seed data, 3 blog posts from existing MDX files, 9 media assets with real local file sizes, and a seed version setting. Verification: `npx prisma migrate status`, `npx prisma validate`, `npx prisma generate`, `npx prisma db seed`, local `tsc --noEmit`, local `eslint . --max-warnings 0`, and `next build` pass. |

## 2026-07-06 Progress Addendum

| Step | Category | Status | Completed | Notes |
|------|----------|--------|-----------|-------|
| 2.1.3 | Database Setup | ✅ Done | 2026-07-06 | CMS/media schema models defined in `prisma/schema.prisma`: `BlogCategory`, `BlogPost`, `Newsletter`, `MediaAsset`, `AiPrompt`. Added `BlogPostStatus`, `NewsletterStatus`, `MediaKind`, `AiTool`; wired `MediaAsset` relations into existing product media placeholders; added FK indexes, `tsvector` fields, GIN FTS indexes, and `pg_trgm` trigram indexes. Verification: `npx prisma validate`, `npx prisma generate`, local `tsc --noEmit`, local `eslint . --max-warnings 0`, and `next build` pass. `pnpm typecheck` could not start in the non-TTY shell because pnpm requested modules-purge confirmation, so the local TypeScript binary was run directly. |
| 2.1.4 | Database Setup | ✅ Done | 2026-07-06 | Operations schema models defined in `prisma/schema.prisma`: `AuditLog` and `SystemSetting`. Added `AuditLog.actorId` FK to `User`, `actorRole` snapshot via `RoleKey`, `action`, `entityType`, `entityId`, `before`, `after`, `ipHash`, `userAgent`, and `createdAt`; added `SystemSetting.key` PK, JSON `value`, `updatedAt`, and `updatedBy` FK to `User`. Indexed `AuditLog.entityType + entityId`, `actorId`, `createdAt`, and `SystemSetting.updatedBy`. Verification: `npx prisma validate`, `npx prisma generate`, local `tsc --noEmit`, local `eslint . --max-warnings 0`, and `next build` pass. Migrations/grants are reserved for Step 2.1.5. |

## 2026-07-08 Phase 1 Closeout (Pre-Phase-2 Gate)

| Item | Category | Status | Notes |
|------|----------|--------|-------|
| Security: API rate limiting | 1.11.x fix | ✅ Done | `server/rate-limit.ts` (in-memory fixed-window, 10 req/min/IP) applied to `/api/contact`, `/api/quote`, `/api/newsletter`. Returns HTTP 429 + `Retry-After`. No PII/IP logged. Decision D-029; limitation I-015. |
| Env: DATABASE_URL optional | 2.1.x fix | ✅ Done | `server/env.ts` no longer requires `DATABASE_URL` at boot, so a Phase 1 static deploy does not fail without a database. Phase 2 DB code must assert it (I-015 context). |
| Docs: stale baseline/context regenerated | Docs | ✅ Done | `IMPLEMENTATION_BASELINE.md` and `docs/10_AI_AGENT_CONTEXT.md` regenerated to real state. |
| Docs: known issues closed | Docs | ✅ Done | I-005 (Motion unused) and I-012 (Database not initialized) moved to Resolved (I-R14, I-R15). Added I-015 (rate-limit scale-out) and I-016 (unverified Phase 1 gates). |
| Docs: AGENTS.md index | Docs | ✅ Done | §14 now lists all 7 tracking docs (06/07/08/09/10, IMPLEMENTATION_BASELINE, PROJECT_FREEZE). |
| Docs: master plan paths | Docs | ✅ Done | `app/marketing/...` corrected to `app/(marketing)/...`; `features/product/_data.ts` (solutions) corrected to `features/solutions/_data.ts`. |
| Verification: typecheck | Build | ✅ PASS | `pnpm typecheck` — zero errors. |
| Verification: lint | Build | ✅ PASS | `pnpm lint` — zero warnings. |
| Verification: build | Build | ✅ PASS | `pnpm build` — 48 routes, all prerendered. |
| Verification: Lighthouse/axe/Rich Results | Build | ⬜ NOT RUN | No local CLI available; asserted via code analysis only (I-016). |
| Verification: live email | Build | ⬜ NOT RUN | Requires `RESEND_API_KEY`; dev stub logs instead. |
| 2.1.6 | Database Setup | ✅ Done | 2026-07-08 | `server/db.ts` created — module-level `PrismaClient` singleton with `globalThis` guard. Prisma 7 requires a runtime driver adapter, so `@prisma/adapter-pg` + `pg` were added as dependencies and wired via `new PrismaPg(process.env.DATABASE_URL ?? '')`. `DATABASE_URL` stays optional at the base env layer (lazy connect; fails clearly on first query if absent). `import 'server-only'` omitted (package not installed; file lives in `server/`). Verification: `pnpm typecheck`, `pnpm lint`, `pnpm build` pass; `prisma validate` + `prisma generate` pass. Live DB query not executed (no DB connectivity test run this turn). |
| 2.2.1 | Admin Auth & RBAC | ✅ Done | 2026-07-10 | **Decision-only step.** Selected **Auth.js (NextAuth v5) + Prisma Adapter** over Clerk (vendor lock-in/cost) and custom auth (security burden); Lucia considered but rejected (archived/unmaintained). Recorded as decision D-034 in `docs/07_DECISIONS.md`; D-014 updated from TBD to selected. Provider mirrors the existing `User`/`Session`/`Role`/`UserRole` schema. No packages installed, no code/routes/middleware/UI created. Next: 2.2.2 (provider install + base config) only after this decision. |
| 2.2.2 | Admin Auth & RBAC | ✅ Done | 2026-07-10 | **Base install + config — REPLACED Auth.js with Better Auth (D-035).** The Auth.js base (installed earlier this same day) compiled but could not work at runtime because the adapter expects `emailVerified`/`image`/`sessionToken` while ARIOT uses `emailVerifiedAt`/`avatarUrl`/`tokenHash`. Removed `next-auth` + `@auth/prisma-adapter`, deleted root `auth.ts` and `app/api/auth/[...nextauth]/route.ts`, removed `AUTH_SECRET`. Installed `better-auth@1.6.23`; created `server/auth.ts` (Prisma adapter on existing `server/db.ts`, `modelName`/`fields` mapping, `secret`/`baseURL` from typed env) and `app/api/auth/[...all]/route.ts` (`toNextJsHandler`). Added `emailVerified Boolean` to `User`, `updatedAt` to `Session`; redesigned `Account` (Better Auth shape) and replaced `VerificationToken` with `Verification`. Env: `BETTER_AUTH_SECRET`/`BETTER_AUTH_URL` added to `server/env.ts` + `.env.example`. **Migration generated + applied to LOCAL dev DB** (`prisma/migrations/20260710081202_auth_better_auth_foundation`). **Runtime DB smoke test PASSED on local dev DB.** NOT done: providers, login UI, RBAC, middleware, admin routes, password login, seeding. |
| 2.2.3 | Admin Auth & RBAC | ✅ Done | 2026-07-10 | **Google OAuth provider + sign-in UI shell (Step 2.2.3).** Added `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` to typed `server/env.ts` (both-or-neither validation; optional at boot; no logging). Configured Better Auth `socialProviders.google` only when both creds present: `disableSignUp: true` (unknown Google users cannot create ARIOT accounts), identity-only scopes (`openid`/`email`/`profile`), no One Tap, no Drive/Calendar/Gmail/contacts. Removed `trustHost`; added explicit `baseURL` (BETTER_AUTH_URL ?? NEXT_PUBLIC_SITE_URL) + `trustedOrigins` (no wildcards). Created shared Better Auth React client `lib/auth-client.ts`. Created sign-in route `app/(auth)/sign-in/page.tsx` (server) + `sign-in-form.tsx` (client): "Admin sign in" heading, internal-access note, "Continue with Google" button, loading/error/provider-unavailable states, safe `?error=` mapping (no secrets/stack traces), return-to-site link. Added minimal `getSession()` server helper (authentication only — no roles/permissions/RBAC). No Prisma schema or migration change. Verification: `tsc`/`eslint`/`next build`/`prisma validate`/`prisma generate` PASS. Runtime (local, built app): `/sign-in` returns 200 with provider-unavailable state; `/api/auth/get-session` returns 200/`null` when unauthenticated (auth server initializes). **LIVE GOOGLE OAUTH NOT TESTED** — no Google credentials available in this environment. |
| 2.2.4 | Admin Auth & RBAC | ✅ Done | 2026-07-10 | **Server-side RBAC guardrails (Step 2.2.4).** Created `server/auth/errors.ts`: typed `AuthenticationError` (401 `AUTHENTICATION_REQUIRED`) and `AuthorizationError` (403 `AUTHORIZATION_DENIED`), both extending `AuthzError` with `code`/`httpStatus`; no `NextResponse`; distinguishable by `instanceof`/`code`. Created `server/auth/permissions.ts`: `AuthorizationContext` interface (userId, email, roles: RoleKey[], permissions: string[]). `getAuthorizationContext()` — returns null for anonymous, non-ACTIVE, or soft-deleted users. `requireAuthenticatedUser()` — throws 401. `requireRole(roles, headers?)` — ANY semantics; throws 403. `requirePermission(perms, headers?)` — ALL semantics; throws 403. `requireAnyPermission(perms, headers?)` — ANY semantics; throws 403. DB query: `User.findUnique` with `userRoles → role { key, permissions }` select (no N+1). Updated `server/auth.ts::getSession()` to accept optional `Headers`. **Discovery:** seed assigns `"*"` to SUPER_ADMIN (wildcard); handled explicitly. No schema/migration change. |
| 2.2.5 | Admin Auth & RBAC | ✅ Done | 2026-07-10 | **Secure admin bootstrap (Step 2.2.5).** CLI script `scripts/bootstrap-admin.ts` runs via `pnpm admin:bootstrap [--apply] [--production]`. Dry-run by default; writes require `--apply`; non-local DB requires `--production`. Zod-validates `BOOTSTRAP_ADMIN_EMAIL` (normalize → trim+lowercase), `BOOTSTRAP_ADMIN_CONFIRM` (literal `GRANT_SUPER_ADMIN`), `BOOTSTRAP_ADMIN_NAME` (optional). Preflight: DB connect, SUPER_ADMIN role must exist, user must not be soft-deleted/suspended. Writes (transaction): `User` created (emailVerified=true, no passwordHash, no Account, no Session) or reused; `UserRole` idempotent (ON CONFLICT DO NOTHING); system `AuditLog` row (actorId=null, action=ADMIN_BOOTSTRAPPED). Better Auth `account.accountLinking` configured (enabled, implicit linking, no different-email, no profile overwrite). OAuth tokens encrypted at rest (`encryptOAuthTokens: true`, uses BETTER_AUTH_SECRET, D-040). Added `@types/pg` devDep. No schema/migration change. Local verification: dry-run→no rows; apply→all assertions pass; re-apply→no-op for User/UserRole; production guard→exit 1 without --production; cleanup→DB restored. **LIVE GOOGLE OAUTH NOT TESTED** — no credentials. |

## 2026-07-10 Pre-Auth Gate Cleanup

| Item | Category | Status | Notes |
|------|----------|--------|-------|
| Tracking docs aligned | Docs | ✅ Done | `docs/10_AI_AGENT_CONTEXT.md`, `docs/IMPLEMENTATION_BASELINE.md`, `docs/06_PROGRESS_LOG.md`, `docs/09_CHANGELOG.md`, and `docs/08_KNOWN_ISSUES.md` now agree that Phase 2 is 6/43 complete and the next step is 2.2.1. Auth/RBAC is explicitly not started. |
| Auth roadmap hardened | Docs | ✅ Done | `docs/05_IMPLEMENTATION_MASTER_PLAN.md` makes Step 2.2.1 decision-only and separates provider install/config, session UI, RBAC, and admin bootstrap work. Weak-agent guardrails added. |
| Env and mail guard fixed | Config | ✅ Done | `server/env.ts` parses `RESEND_API_KEY`, exposes `getDatabaseUrl()`, and keeps `DATABASE_URL` optional at base boot. `server/mail/client.ts` now throws on missing production email config and keeps dev stub logs non-PII. |
| Prisma env access fixed | Config | ✅ Done | `server/db.ts` now uses typed env helpers instead of direct `process.env` reads while preserving Prisma 7 adapter compatibility. |
| Security docs aligned | Docs | ✅ Done | `docs/TECH_ARCHITECTURE.md` documents the actual CSP trade-off and server-only convention; `docs/07_DECISIONS.md` records the env, server-only, and auth split decisions. |
| Root artifact removed | Cleanup | ✅ Done | Removed zero-byte `nul` file from repository root. |

**Remaining unverified Phase 1 gates (I-016):** Lighthouse >= 90, LCP <= 2.5s, axe-core zero violations, Google Rich Results validation, live email delivery. All require tooling/keys not present in this environment.

| 2.3.1 | Admin Shell | ✅ Done | 2026-07-10 | **Admin layout shell (Step 2.3.1).** `app/(admin)/layout.tsx` is the server-side security boundary: `getAuthorizationContext()` → redirect `/sign-in` if no session, redirect `/` if authenticated but lacking an admin role (SUPER_ADMIN/CONTENT_ADMIN/SUPPORT_ADMIN/SALES_ADMIN). `components/admin/admin-shell.tsx` (client) renders top bar (logo, breadcrumb from pathname, disabled search placeholder, notifications empty-state popover, profile menu with role chips + server-action sign-out) and left rail (sticky desktop / drawer mobile) from `components/admin/admin-nav.tsx` (single source; only Overview has a route, rest are honest "Soon" disabled items). `app/(admin)/actions.ts` server action `adminSignOut` calls `auth.api.signOut` then redirects `/sign-in`. `app/(admin)/admin/page.tsx` Overview placeholder (real copy: access-verified banner, module cards, getting-started steps). No middleware added (smallest server-side guard per plan §2.2). **Not runtime-tested with a live session** (no Google creds / no bootstrapped admin in local DB). I-019 open. |

| 1.0.1 | Visual Redesign | ✅ Done | 2026-07-10 | **Phase 1 Visual Redesign & Premium Light-Theme Correction.** Scoped light token system behind `.theme-light` (white/`#F7F9FC` surfaces, navy `#0F172A` text, slate `#475569` secondary, `#E2E8F0` borders, blue `#2563EB` accent). Applied to `(marketing)` + `(auth)/sign-in` route groups only; admin (`app/(admin)`) keeps dark tokens untouched. `globals.css` `.theme-light` block re-binds all token vars; `lib/design-tokens.ts` mirror updated to match. Marketing layout wrapped in `.theme-light`; sign-in `main` tagged `.theme-light`. Recolored hero SVGs (21x9 + 9x16) and 3D robot to light palette. Fixed Card `glass` border (`white/[0.06]` → `steel-700`). No auth/RBAC/DB/admin code changed. `tsc`/`eslint`/`next build` pass. **Not browser-verified** (no headless browser in env); CSS cascade confirmed via compiled output. |

| 1.0.4 | IA & Mobile | ✅ Done | 2026-07-10 | **Public IA, mobile nav, and new pages (corrective step 1.0.4).** Updated `SiteHeader` nav: Home(`/`), R&D(`/research`), Workspace(`/workspace`), Components(`/components`), Solutions(`/solutions`), About(`/about`) + `MoreMenu` client component (Blog, Support dropdown, closes on route change/outside click). Mobile drawer hardened: `w-[88vw] max-w-[360px]`, Blog+Support added to mobile nav, inherits `.theme-light` so visually light. `SiteFooter` reorganised: columns Company/R&D/Workspace/Components/Solutions/Resources; dead links (`/innovation-lab`, `/about/press`, `/careers`, `/support/ticket`) removed; placeholder email/phone display removed; tagline de-bracketed; copyright updated to "ARIOT Technologies." New static routes: `app/(marketing)/research/page.tsx`, `app/(marketing)/workspace/page.tsx`, `app/(marketing)/components/page.tsx` (all honest, no fake commercial claims, correct status badges). Homepage rewritten: 10 sections with approved "Building the Robotics Ecosystem of Bangladesh." positioning, business-areas grid, workspace+components sections, honest metrics (status labels not invented numbers). `_home-content.ts` rewritten: brackets removed, `BUSINESS_AREAS`/`COMPONENT_TEASERS` added. About/solutions pages de-bracketed. `tsc` ✅ · `eslint` ✅ · `next build` ✅ · `/research` `/workspace` `/components` all present as `○` (static). No auth/RBAC/DB/admin code changed. NOT browser-verified (no headless browser). I-019 open. |

| 1.0.5 | Drawer & Polish | ✅ Done | 2026-07-10 | **Mobile drawer portal fix, glassmorphism system, full page polish (corrective step 1.0.5).** Root cause confirmed: sticky header `backdrop-filter` creates containing block for fixed-position descendants in WebKit, confining drawer to header bounds (~60px). Fix: `createPortal` to `document.body`; panel `absolute inset-y-0 right-0 w-[88vw] max-w-[360px]` in `fixed inset-0 z-[200]` root; `theme-light` applied directly to panel. `.glass-panel`/`.glass-panel-strong` CSS utilities + `drawer-slide-in` keyframe added. Card glass variant updated to proper backdrop-blur. More-menu glass dropdown. Homepage hero: glass engineering-status overlay on 3D container. R&D/workspace: glass status/rental panels. All main public pages fully de-bracketed: products, contact, quote, blog, support, sign-in. Sign-in: glass card + blueprint grid background. `tsc` ✅ · `eslint` ✅ · `next build` ✅. **Browser-verified 2026-07-10** at 390×844 and 360×800 by project team. Confirmed: drawer opens from right at ~88vw, no black panel, all nav links and CTAs visible, X/Escape/overlay/nav-link close work, body scroll locked, homepage/R&D/workspace/components/products/solutions/about/contact/sign-in render correctly, admin dark and functional. I-024 closed. I-023 open (secondary detail pages). |

| 2.3.2 | Admin Navigation | ✅ Done | 2026-07-10 | **Admin navigation structure (Step 2.3.2).** Typed grouped config in `components/admin/admin-nav.tsx` (`AdminNavEntry` union: `kind:'link'` for Overview, `kind:'group'` for 6 groups × leaf items). Single source of truth: Overview(`/admin`), Catalog(Products, Categories), Sales(Orders, Quotes, Customers), Support(Tickets, Articles), Content(Blog, Media), Operations(Users, Roles, Audit Log), Settings(Settings). All non-Overview leaves carry `soon:true` + their intended `href` (for breadcrumb) — rendered as disabled spans, never 404-linked. New `components/admin/admin-nav-rail.tsx` client component: collapse/expand per group (all default-open, in-session), `isLeafActive` exact matching for Overview / prefix for others, `aria-current="page"` on active leaf, disabled items non-focusable with "Soon" badge (text not colour-only). Updated `components/admin/admin-shell.tsx`: imports `AdminNavRail`, adds `flex flex-col` to aside, replaces old flat nav (shell: 333→297 lines). Breadcrumb `breadcrumbForPath` updated to traverse grouped structure. No auth/RBAC/Prisma/public-nav changed. `tsc` ✅ · `eslint` ✅ · `next build` ✅ · `/admin` still ƒ dynamic, RBAC guard intact. Not browser-verified. |

| 2.3.3 | Admin Theme & Density | ✅ Done | 2026-07-10 | **Admin theme, density, and semantic status system (Step 2.3.3).** `.theme-admin` class on shell root: explicit dark admin theme boundary (re-declares core surface tokens so admin can never accidentally adopt `.theme-light`). CSS density variables `--adm-row-py`/`--adm-leaf-py`/`--adm-gutter`/`--adm-content-py` in globals.css under `.theme-admin` + `.theme-admin[data-density="compact"]`. Comfortable target: ~40px rows/16px gutter; compact: ~32px rows/12px gutter. Mobile tap-target exception: compact restores comfortable padding at < 1024px. `data-density` attribute driven by localStorage-persisted state (key `ariot-admin-density`; default comfortable; validated on restore; localStorage errors silently ignored). `AdminDensityToggle` two-button segmented control in profile popover (`aria-pressed`, keyboard-accessible). `AdminStatusChip` dark semantic primitive (neutral/info/success/warning/danger/pending/inactive; text label always present; optional dot; sm + default sizes). `admin-shell-ui.tsx` extracted (Popover, EnvironmentChip, initials) to keep shell under 300 lines. `adm-nav-link`/`adm-nav-group-btn`/`adm-nav-leaf` CSS classes added to nav-rail for density-sensitive padding. Overview page uses `AdminStatusChip` for module cards + access-verified badge. `tsc` ✅ · `eslint` ✅ · `next build` ✅. No auth/RBAC/Prisma/public-nav changed. Not browser-verified. |

| 2.3.4 | Notifications Panel | ✅ Done | 2026-07-10 | **Admin notifications panel foundation (Step 2.3.4).** Minor correction: density label "Comfy" → "Comfortable" in `AdminDensityToggle`. Typed model: `AdminNotification` interface (id, title, description?, createdAt, read, severity?, href?) + `AdminNotificationSeverity` union in `components/admin/admin-notifications-panel.tsx`. `AdminNotificationsPanel` component: `absolute` positioned below trigger, `w-[360px] max-w-[calc(100vw-2rem)]` for mobile safety, `role="region" aria-label="Notifications"`, header with unread count, scrollable content area (`max-h-[400px]`), polished empty state ("You're all caught up" with honest description). `NotificationItem` component: handles read/unread, severity dot + sr-only label, absolute date (`Intl.DateTimeFormat en-US`), optional `href` → Next.js Link. Unread badge on trigger: shown only when `NOTIF_UNREAD > 0` (currently always 0 — no fake count), format 9+/99+. `admin-shell.tsx`: `aria-haspopup="true"`, `aria-label="Open notifications"`, `aria-expanded` on trigger; inline popover replaced with `AdminNotificationsPanel`. `EMPTY_NOTIFICATIONS = []` + `NOTIF_UNREAD = 0` at module level — no fake/seeded data. Shell: 296 lines. `tsc` ✅ · `eslint` ✅ · `next build` ✅. No backend/DB/auth/public-nav changed. Not browser-verified. |

| 2.4.1 | Admin Data Table | ✅ Done | 2026-07-10 | **Shared admin data-table foundation (Step 2.4.1).** Three files: `components/admin/data-table-types.ts` (all exported types — 151 lines); `components/admin/data-table-states.tsx` (SortIcon, SkeletonRows, EmptyRow, ErrorRow — 104 lines); `components/admin/data-table.tsx` (DataRow, DataTablePagination, DataTable — 289 lines). Fully controlled: parent provides rows, sort, filter, pagination, loading, error. Generic over `T` — no `any`. Column API: id, header, cell(row)→ReactNode, sortKey?, align?, width?, minWidth?, hideBelow?, className?, headerClassName?. Sort cycle: none→asc→desc→asc; fires `onSortChange(DataTableSort)`. `aria-sort` on th; sort buttons keyboard-operable with cyan focus ring. Cursor pagination: opaque cursors; Previous/Next buttons disabled when unavailable/loading. Loading: skeleton rows (animate-pulse bars, varying widths). Empty: "No records found" with Inbox icon, parent-overridable. Error: "Unable to load records" with AlertCircle, optional retry. Density: `--adm-cell-px/py/thead-py` CSS vars in globals.css + `.adm-td`/`.adm-th` classes. Row interaction: opt-in via `onRowActivate`; Enter key activates; clicks on child interactive elements don't bubble. Responsive: `overflow-x-auto` container, `min-w-[600px]` table, `hideBelow` responsive column hiding. Native table semantics throughout (`<table>`, `<caption>`, `scope="col"`, etc.). No external grid library. No feature pages, no fake business data. `tsc` ✅ · `eslint` ✅ · `next build` ✅. Not browser-verified. |

| 2.4.2 | Admin Products List | ✅ Done | 2026-07-10 | **Admin products list (Step 2.4.2).** Pre-step DataTable hardening: keyboard `e.target !== e.currentTarget` fix + pagination count semantics (`rowCount={rows.length}`, hides during loading). Admin nav: Products leaf enabled (removed `soon:true`, permission corrected to `products.read`). `server/admin/products/list-products.ts` (208L): `requirePermission('products.read')` before DB; Prisma cursor pagination (keyset forward/backward, P2001 stale-cursor → first page fallback); search via `contains/insensitive` on name/sku/slug (NOT searchVector — I-019); allowlisted sort (name/sku/status/stock/updatedAt); BigInt `priceMinor` → string in DTO; `deletedAt:null` soft-delete filter. `app/(admin)/admin/products/page.tsx` (96L): `force-dynamic`, parse+validate URL params, catches auth/query errors. `components/admin/products/products-table.tsx` (235L): 7 columns (thumbnail+name+slug, SKU mono, category, AdminStatusChip status, stock, formatted price, updated date), Toolbar (search/status/category filters + clear), `useTransition`+`router.push` navigation, cursor-aware URL (resetCursor on filter/sort change). Read-only — no mutations. `tsc` ✅ · `eslint` ✅ · `next build` ✅ · `/admin/products` ƒ dynamic. Not browser-verified. Local DB has 6 seeded DRAFT products but no active session to test with. |

| PLAN-1 | Architecture | ✅ Done | 2026-07-10 | **Corrective planning: complete admin scope, homepage CMS, promotions, workspace booking architecture.** Formalized the complete Phase 2 extended admin scope across 7 new categories (2.10–2.16) plus corrective steps C.1–C.2. Key additions: R&D project management (2.10), structured homepage CMS without arbitrary HTML injection (2.11), central promotions engine with placement rules (2.12), workspace plans + online booking in 3 stages (2.13), component catalog using shared Product model + inventory (2.14), SEO management (2.15), content expansion: news/pages/navigation/FAQs (2.16). ADMIN_DASHBOARD_PLAN updated with complete IA (all nav sections documented, permission matrix extended, planned phase table). DATABASE_SCHEMA_PLAN updated with planned models: HomepageConfig, HomepageSection, Promotion, PromotionPlacement, Coupon, WorkspacePlan, Booking, AvailabilityRule, BlackoutPeriod, StockMovement, RdProject, RdUpdate. FEATURE_ROADMAP Phase 2 scope extended. Decisions D-056–D-061 added. Issues I-019 severity elevated to CRITICAL (blocks all schema changes), I-025 permission wildcard mismatch, I-026 schema blocker documented. No source code changed. No Prisma schema/migration changed. |

| C.2 | Corrective | ✅ Done | 2026-07-10 | **Corrective Step C.2 — Resolve I-019 generated-column drift.** Root cause confirmed: `Product.searchVector` and `BlogPost.searchVector` were `GENERATED ALWAYS AS (...) STORED tsvector` columns in init migration but declared as plain `Unsupported("tsvector")?` in schema.prisma without a generated expression. Prisma 7 diff generated `ALTER COLUMN searchVector DROP DEFAULT` (PostgreSQL ERROR 42601 — invalid on GENERATED columns). Fix: dropped both columns and their GIN indexes via `prisma/migrations/corrective_c2_drop_search_vectors/migration.sql`. Schema.prisma updated (removed both field declarations and `@@index([searchVector])` references). No user data lost — columns were derivative caches from name/tagline/description/title/body fields (all already trgm-indexed for ILIKE search). Pre-correction baseline: 6 products, 6 categories, 3 blog posts, 4 roles, 1 user, 9 media assets. Post-correction counts: identical. **Stable backup**: `C:\Users\princ\backups\ariot\ariot_pre_c2_20260725_201317.dump` (77.4 KB, pg_dump custom format, PostgreSQL 18.3, pg_restore --list verified: 190 TOC entries). **Final verification (2026-07-25)**: Migration record verified (exactly 1 record, applied, no rollback, correct order). Prisma `migrate diff` 4-way comparison: (A) migrations→database: empty, (B) schema→database: empty, (C) migrations→schema: empty, (D) migrations→fresh disposable replay: all 3 applied cleanly. Disposable `prisma migrate dev --create-only` probe succeeded (generated only the temporary probe change). Probe field and migration completely removed — no artifacts. Primary DB re-verified: row counts unchanged (Product:6, Category:6, BlogPost:3, Role:4, User:1, MediaAsset:9, AuditLog:0). Product IDs/slugs unchanged. No searchVector columns/indexes remain. 5 trigram GIN indexes confirmed present. `prisma validate` ✅ · `prisma generate` ✅ · `prisma migrate status` = "up to date" (3 migrations) ✅ · `tsc` ✅ · `eslint --max-warnings 0` ✅ · `next build` ✅ · `git diff --check` ✅. I-019 closed. I-026 closed. D-062 added. Future `prisma migrate dev` **directly proven** to work. |

**Recommended next implementation step:** C.2 ✅ complete (final verification passed 2026-07-25). Next: **Corrective Step C.1** — fix permission wildcard mismatch (`products.*` vs exact `products.read`) so CONTENT_ADMIN can access the products admin. Then resume **Step 2.4.3** — Product editor Details tab.

| C.1 | Corrective | ✅ Done | 2026-07-25 | **Corrective Step C.1 — Resolve I-025 permission wildcard mismatch.** All namespace wildcard permissions (`products.*`, `categories.*`, `blog.*`, `media.*`, `support_articles.*`, `tickets.*`, `customers.*`, `quotes.*`, `orders.*`) replaced with explicit permission strings across all 4 roles. Created `server/auth/permission-catalog.ts`: centralized permission constants, `ROLE_PERMISSION_SETS`, `WILDCARD_EXPANSION` mapping, pure `hasPermission`/`hasAllPermissions`/`hasAnyPermission` evaluators. Updated `server/auth/permissions.ts`: `requirePermission` and `requireAnyPermission` now delegate to pure evaluators. Updated `prisma/seed.ts`: all role permission arrays use explicit values. Created `scripts/reconcile-role-permissions.ts`: offline idempotent CLI (dry-run default, --apply for writes, --production for non-local DB, transactional, AuditLog per changed role). Created `tests/permission-evaluator.test.ts`: 36 focused tests covering exact match, wildcard denial, global `"*"`, role sets, structural validation. Local DB reconciled: CONTENT_ADMIN 5→10 perms, SUPPORT_ADMIN 4→9, SALES_ADMIN 5→10. SUPER_ADMIN unchanged (`["*"]`). 3 AuditLog entries created. Counts: Role:4, UserRole:1, User:1, Product:6 — all unchanged. Backup: `C:\Users\princ\backups\ariot\ariot_pre_c1_20260725_205020.dump`. Second apply = no-op. CONTENT_ADMIN now satisfies `products.read` (proven by evaluator test + DB inspection). `tsc` ✅ · `eslint --max-warnings 0` ✅ · `next build` ✅ (53 routes) · Permission tests 36/36 ✅. No schema/migration change. I-025 resolved. D-063 added. |

**Recommended next implementation step:** C.1 ✅ complete, C.2 ✅ complete. Both corrective steps done. Next: **Step 2.4.3** — Product editor Details tab.

| 2.4.3 | Admin Products | ✅ Done | 2026-07-25 | **Product editor Details tab (Step 2.4.3).** Created `/admin/products/[id]` with server-side product loading, permission-gated view/edit modes, and Details tab. **Strict input contract**: Zod `.strict()` rejects unknown/deferred/lifecycle fields atomically. **Status excluded from autosave** (D-064). **Production queue**: editor uses tested `createSaveQueue()` from `lib/admin/product-save-queue.ts` — no duplicate inline queue logic. At most one request active; pending edits queued; manual Save uses same path; conflict stops retry. **Authorization**: production wrapper enforces `products.write` before internal executor; 8 authorization contract tests prove gating. **Server**: `get-product.ts`, `update-product-details.ts` (wrapper + `executeProductDetailsUpdate`), `product-details-schema.ts` (strict Zod). **Editable fields**: name, slug, sku, tagline, description, brand, categoryId, salesType, priceMinor, currency. **Tests**: 85 unit (36 perm + 30 validation + 11 queue + 8 auth) + 11 PostgreSQL integration = 96 total, all pass. `tsc` ✅ · `eslint` ✅ · `next build` ✅. No schema/migration change. |

**Recommended next implementation step:** Step 2.4.3 ✅ complete. Next: **Step 2.4.4** — Product editor Media tab, or continue with remaining product CRUD steps per the master plan.

| 2.4.3-closure | Admin Products | ✅ Done | 2026-07-26 | **Step 2.4.3 final authorization orchestration closure.** (1) Created shared `createAuthorizedProductDetailsUpdater()` factory in `server/admin/products/authorized-product-update.ts` — single orchestration function used by BOTH production and tests. (2) Refactored production `updateProductDetails` to be created by this factory (with real `requirePermission` + real executor). (3) Removed the separate test-only `createTestableWrapper()` that duplicated authorization logic. (4) New authorization orchestration tests (12 tests): products.write permits, global `*` permits, products.read denies, empty denies, products.* denies, unknown denies, invalid context denies, executor failure sanitized, actor/input passthrough, multi-permission combinations. (5) New denied-write database integration test (4 tests) using disposable PostgreSQL (`ariot_auth_denial_test`): products.read-only denied with verified Product/AuditLog/row-count unchanged; empty permissions denied; products.* denied; global `*` permitted + restored. (6) API route security contract test (13 tests): verifies route imports only `updateProductDetails`, accepts no actor/permission/role fields, exposes no internal errors. (7) Updated existing integration test to self-manage disposable DB lifecycle. **Test totals**: 102 unit (36 perm + 30 validation + 11 queue + 12 auth + 13 API security) + 15 integration (11 mutation + 4 denied-write) = 117 total, all pass. `prisma validate` ✅ · `prisma generate` ✅ · `tsc --noEmit` ✅ · `eslint --max-warnings 0` ✅ · `next build` ✅ (54 routes). No schema/migration change. Disposable DBs dropped after tests. |

**Recommended next implementation step:** Step 2.4.3 ✅ final closure complete (2026-07-26). Next: **Step 2.4.4** — Product editor Media tab.

| 2.4.4 | Admin Products | 🟨 In Progress | 2026-07-26 | **Product editor Media tab (Step 2.4.4).** Created `/admin/products/[id]/media` with server-side media loading, permission-gated view/edit modes. **Supported features**: Hero image set/clear, hero video set/clear, gallery image add/remove/reorder, gallery video add/remove, existing media selector with server-backed search + cursor pagination, MIME type validation (only approved image/video types), optimistic concurrency (Product.updatedAt), transactional audit logging, read-only mode, 3D model non-functional placeholder. **Schema relationships used**: Product.heroImageId, Product.heroVideoId, ProductImage (join model with order/altText/isPrimary), ProductVideo (join model with order/caption). **Architecture gap**: No storage provider is configured (decision D-015 deferred S3-compatible storage). No signed-URL signing service, no upload endpoint, no upload-completion verification. Upload section shows clearly disabled state. Existing MediaAsset records (seeded) can be selected and assigned. **Tests**: 140 total (102 existing + 12 media validation + 11 integration + 15 DB). `tsc` ✅ · `eslint` ✅ · `next build` ✅ (57 routes). No schema/migration change. |

**Recommended next implementation step:** Step 2.4.4 🟨 in progress — storage provider selection and upload flow implementation required to complete. Consider adding an approved S3-compatible provider (per D-015) before marking complete. Alternatively: proceed to **Step 2.4.5** (Variants tab) if upload is non-blocking.

| 2.4.4-closure | Admin Products | 🟨 In Progress | 2026-08-05 | **Step 2.4.4 final closure — STORAGE-1R Cloudflare R2 uploads (D-067).** Implemented the complete secure upload pipeline with no new dependencies: **initiate** (`/api/admin/media/upload/initiate`, `media.write`) → Zod-validated input, image/video size limits (10 MB / 200 MB), canonical keys from a preallocated MediaAsset id, minted 15-min HMAC-SHA256 completion token (`v1.<payload>.<hmac>`, jti ≥128 bits, binds mediaAssetId/keys/mime/size/kind/**user**), 5-min presigned PUT with Content-Type bound → browser uploads directly to private `tmp/uploads/{yyyy}/{mm}/{id}.{ext}` → **complete** (`/api/admin/media/upload/complete`, `media.write` **and same initiating user**) verifies token + keys, HEAD (exact size+MIME+ETag), ≤64 KiB ranged GET with IfMatch, real file-signature detection (file-type, 6 approved formats), CopyObject promote to immutable `public/products/{images|videos}/{yyyy}/{mm}/{id}.{ext}` (`Cache-Control: public, max-age=31536000, immutable`), reverify, then MediaAsset + AuditLog in one transaction; `MediaAsset.id` is the idempotency boundary. Storage split into 5 modules under 300 lines (`upload-token.ts`, `upload-keys.ts`, `upload-context.ts`, `upload-initiate.ts`, `upload-persist.ts`, `upload-complete.ts`; `storage-service.ts` is a stable barrel). Env: typed `R2_*` config + `MEDIA_UPLOAD_TOKEN_SECRET` in `server/env.ts` + `.env.example`. **Tests**: 181/181 pass via `node --experimental-strip-types` (13 token + 24 keys + 8 completion-logic + route contracts). `tsc` ✅ · `eslint` ✅ · `next build` ✅ (58 routes). No schema/migration change. **REMAINING (I-027)**: real R2 dev-bucket smoke test not run — no R2 credentials in local `.env`; runbook in `docs/CLOUDFLARE_R2.md`. Step stays 🟨 until smoke test passes; **do not start Step 2.4.5 until then**. |

| 2.4.4-security | Admin Products | 🟨 In Progress | 2026-08-17 | **Step 2.4.4 R2 upload implementation security correction pass (STORAGE-1R / D-067).** Closed all 13 defects flagged in the post-2026-08-05 audit: (D1) removed client-controlled `userId`; (D2) removed client-controlled `mediaAssetId`; (D3) initiation contract is strict Zod `{filename, mimeType, sizeBytes, kind}`; (D4) MediaAsset.id now uses `cuid` matching Prisma's `cuid()` default; (D5) presigned PUT (not POST); (D6) all direct package declarations verified; (D7) real `file-type@^21` signature detection; (D8) idempotency on `MediaAsset.id` PK (not in-memory JTI Set); (D9) best-effort `DeleteObject` temp cleanup after promote; (D10) ETag-bound copy via `CopySourceIfMatch` (production code matches the precondition); (D11) **12 PostgreSQL integration tests** in `tests/upload-complete-integration.test.ts` against a disposable DB; (D12) **opt-in R2 smoke test** in `tests/media-upload-r2.smoke.test.ts`; (D13) test totals verified from actual runner output: **159 unit/contract + 34 PostgreSQL integration = 193 pass** (12 of the integration tests are the R2 upload-complete scenarios) + **1 opt-in smoke skipped**. Also fixed: unused `filename` destructure in `upload-initiate.ts` (lint) and a 3-arg `runFinalize` call in the integration test (typecheck). Client `useMediaUpload` rewritten to send only the strict contract. API routes map `not_configured` → HTTP 503. **Tests**: 193/194 pass (159 unit/contract + 34 integration); 1 opt-in smoke correctly skips without credentials. `tsc --noEmit` ✅ · `eslint --max-warnings 0` ✅ (0 warnings) · `next build` ✅ (58 routes). No schema/migration change. Direct deps `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`, `cuid`, `file-type`, `pg` all declared in package.json. **REMAINING (I-027)**: real R2 dev-bucket smoke test still blocked on env creds. Step stays 🟨 until live smoke test passes. |

| 2.4.4-local | Admin Products | ✅ Done | 2026-08-18 | **Step 2.4.4 CLOSED — Media storage provider abstraction, local provider default (D-068).** Added a `MediaStorageProvider` seam and made **`MEDIA_STORAGE_PROVIDER=local` the working default** (R2 code/route/tests retained and switchable via env; `r2` remains fail-closed without full R2 config). **New local provider stack** (`server/storage/`): provider contract (`media-storage-provider.ts`), config + root-safety (`media-storage-config.ts` — absolute-only root, rejects `..`/`public/`), filesystem primitives (`local-storage-fs.ts`), `LocalMediaStorageProvider`, selector with fail-closed resolution (`get-media-storage-provider.ts`), signature verification (`media-file-verification.ts`), and multipart pipeline `local-upload.ts` reusing the SAME verify/promote/persist/compensation flow (10 MB image / 200 MB video caps, real file-signature detection). **Routes**: `POST /api/admin/media/uploads/local` (Content-Length pre-guard → formData), `GET /api/admin/media/upload/mode` (server-resolved provider), `GET /api/admin/media/storage/health`, and public delivery `app/media/[...segments]` (Node runtime, local-provider-only, immutable cache, Range/206/416 support, no listing, `tmp/` unreachable). **Client** `use-media-upload.ts` rewritten provider-agnostic (mode resolved server-side; local multipart with progress/abort; R2 path unchanged; hook interface unchanged). **Persist refactor**: `persistCompletedAsset` now takes a provider-neutral `PersistPayload` + `cdnUrl` option; R2 `finalizeUpload` computes `cdnUrl` from `publicBaseUrl`; `get-product-media.ts` centralizes URL generation via `provider.getPublicUrl`. Env (`server/env.ts` + `.env.example`): `MEDIA_STORAGE_PROVIDER`, `MEDIA_LOCAL_ROOT`, `MEDIA_PUBLIC_BASE_URL`; `.env` set to local default. **Tests**: +45 → **238/239 node:test pass (1 opt-in R2 smoke skipped)** — 15 config/root-safety, 19 validation, 10 PostgreSQL integration (disposable `ariot_local_upload_test`), 1 local end-to-end smoke (self-contained, always-on). `tsc --noEmit` ✅ · `eslint --max-warnings 0` ✅ (0 warnings) · `next build` ✅ (59 routes incl. `/media/[...segments]`). No schema/migration change. Scratch files removed. Runbook + Nginx delivery doc in `docs/LOCAL_MEDIA_STORAGE.md`. |

| 2.4.5-variants | Admin Products | ✅ Done | 2026-08-18 | **Step 2.4.5 CLOSED — Product editor Variants tab.** Added `/admin/products/[id]/variants` (page + tab in `product-editor-header.tsx`) backed by a single dispatch endpoint `POST /api/admin/products/variants` (`action`: `createVariant` / `updateVariant` / `archiveVariant`) gated by `products.write` server-side; read access via `products.read` with `canEdit` surfaced to the client. **Variant model**: free-form `optionValues` key/value pairs (no hard-coded option names) normalized server-side (trim; ≤ 20 keys; key ≤ 40; value ≤ 100; empty `{}` allowed but only once per product); order-independent combination-uniqueness via `optionCombinationKey`; SKU normalized to **uppercase** (trim + collapse whitespace) with **global** uniqueness across all variants (incl. archived) + active products; per-variant name/SKU/priceMinor (non-negative BigInt, BDT/USD)/stock (non-negative Int)/barcode/isDefault. **Archive** = soft-delete `deletedAt` (idempotent, SKU stays reserved). **Concurrency**: `Product.updatedAt` is the aggregate token (`expectedUpdatedAt` in / new token out); stale → 409 conflict. **Audit**: every successful mutation writes AuditLog (entityType `Product`, entityId productId; `PRODUCT_VARIANT_CREATED` / `PRODUCT_VARIANT_UPDATED` / `PRODUCT_VARIANT_ARCHIVED`) + touches `Product.updatedBy`; no-op updates succeed without audit or token change. **UI**: responsive table (min-w 880, horizontal scroll), option chips, inline edit/archive, add-variant form, optimistic updates with conflict banner + Reload, empty + read-only states. **FINAL VERIFICATION (2026-08-18, production-service tests)**: business rules re-verified by executing the **real production executors** through the shared authorization boundary against a disposable `ariot_variants_service_test` PostgreSQL DB (31 tests; controlled writer/reader/super/namespace-wildcard/none contexts; `products.*` denied per D-060; deny → `AuthorizationError` exactly like `requirePermission`). Architecture refactored to wrapper+executor: `variant-auth.ts` (session) → executors (`*-executor.ts`, context-injected, `authorizeProductWrite/Read`). Stale orphan `update-product-variants.ts` deleted. **I-028 fixed** (`product-details-schema.ts` priceMinor uses `ctx.addIssue` + `z.NEVER`; never throws; 11 regression tests). Default-variant invariant reviewed: **deferred** — no DB constraint; zero/multiple defaults possible today (service clears only when setting `true`); documented, no behavior invented. **Totals**: **376/376 node:test pass (1 opt-in R2 smoke skipped)** across **22 files** (+73: 31 real-service + 31 variant-schema lifecycle/I-028 + 11 details I-028). `tsc --noEmit` ✅ · `eslint --max-warnings 0` ✅ · `next build` ✅ (62/62 static pages). No schema/migration change. New runner: `node --import ./tests/helpers/register-hooks.mjs --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test tests/*.test.ts`. New issue **I-029** (PrismaPg timestamptz wall-clock UTC under non-UTC session; harness forces UTC). |
**Recommended next implementation step:** Step 2.4.5 ✅ **Closed** (2026-08-18 — final production-service verification done; **376/376 node:test pass**, 1 opt-in R2 smoke skipped, build green; I-028 resolved; I-029 noted). Next: **Step 2.4.6** — Product editor Specifications tab.
